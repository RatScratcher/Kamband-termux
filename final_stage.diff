diff --git a/cave_patch.diff b/cave_patch.diff
new file mode 100644
index 0000000..f69f914
--- /dev/null
+++ b/cave_patch.diff
@@ -0,0 +1,15 @@
+--- src/cave.c
++++ src/cave.c
+@@ -2568,6 +2568,12 @@ void update_view(void)
+ 		over = MAX_SIGHT * 3 / 2;
+ 	}
+
++	/* Limit vision radius in SECTOR_DARK to 1 */
++	if (cave_sector[py][px] == SECTOR_DARK) {
++		full = 1;
++		over = 1;
++	}
++
+
+ 	/*** Step 0 -- Begin ***/
+
diff --git a/changes.diff b/changes.diff
new file mode 100644
index 0000000..282ec5f
--- /dev/null
+++ b/changes.diff
@@ -0,0 +1,1213 @@
+diff --git a/diff_out b/diff_out
+new file mode 100644
+index 0000000..e1192bc
+--- /dev/null
++++ b/diff_out
+@@ -0,0 +1,398 @@
++diff --git a/kamband_stats.csv b/kamband_stats.csv
++new file mode 100644
++index 0000000..dda3f10
++--- /dev/null
+++++ b/kamband_stats.csv
++@@ -0,0 +1,2 @@
+++turn,event_type,duration_ms,mem_kb,depth,x,y,elevation
+++1,level_gen,6911,10496,29,365,58,0
++diff --git a/lib/data/a_info.raw b/lib/data/a_info.raw
++new file mode 100644
++index 0000000..97245a0
++Binary files /dev/null and b/lib/data/a_info.raw differ
++diff --git a/lib/data/e_info.raw b/lib/data/e_info.raw
++new file mode 100644
++index 0000000..1323fe7
++Binary files /dev/null and b/lib/data/e_info.raw differ
++diff --git a/lib/data/f_info.raw b/lib/data/f_info.raw
++new file mode 100644
++index 0000000..6bc9a4f
++Binary files /dev/null and b/lib/data/f_info.raw differ
++diff --git a/lib/data/k_info.raw b/lib/data/k_info.raw
++new file mode 100644
++index 0000000..f4bc9c7
++Binary files /dev/null and b/lib/data/k_info.raw differ
++diff --git a/lib/data/r_info.raw b/lib/data/r_info.raw
++new file mode 100644
++index 0000000..0b7ee1b
++Binary files /dev/null and b/lib/data/r_info.raw differ
++diff --git a/lib/data/v_info.raw b/lib/data/v_info.raw
++new file mode 100644
++index 0000000..f3abcc6
++Binary files /dev/null and b/lib/data/v_info.raw differ
++diff --git a/lib/save/1001.Jules b/lib/save/1001.Jules
++new file mode 100644
++index 0000000..626d5ca
++Binary files /dev/null and b/lib/save/1001.Jules differ
++diff --git a/load_debug.log b/load_debug.log
++new file mode 100644
++index 0000000..a5effe7
++--- /dev/null
+++++ b/load_debug.log
++@@ -0,0 +1,13 @@
+++rd_savefile_new: start
+++Loading a 2.1.9 savefile...
+++rd_extra: start
+++rd_extra: seeds read
+++rd_extra: death flag read
+++Loaded Turn: 1
+++Starting to restore Dungeon...
+++Restoring Dungeon...
+++Dungeon Header: Depth 29, PY 58, PX 365, YMAX 440, XMAX 462
+++rd_dungeon: start RLE 1 (info)
+++rd_dungeon: start RLE 2 (fire life)
+++rd_dungeon: start RLE 3 (feat)
+++rd_dungeon: start RLE 4 (sector)
++diff --git a/src/generate.c b/src/generate.c
++index 17ac01d..5187da5 100644
++--- a/src/generate.c
+++++ b/src/generate.c
++@@ -315,6 +315,9 @@ struct dun_data
++ 	/* Array of which blocks are used */
++ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+++	/* Array of sector types for each block */
+++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+++
++ 	/* Hack -- there is a pit/nest on this level */
++ 	bool crowded;
++ };
++@@ -707,9 +710,9 @@ static void place_random_door(int y, int x)
++  */
++ static void alloc_stairs(int feat, int num, int walls, bool force_room)
++ {
++-	int y, x, j;
+++	int y, x, j, sy, sx;
++
++-	/* The walls and num parameters are unused */
+++	/* The walls and num parameters are mostly unused in the sector-based version */
++ 	(void)walls;
++ 	(void)num;
++
++@@ -734,7 +737,6 @@ static void alloc_stairs(int feat, int num, int walls, bool force_room)
++     }
++
++ 	/* Allocate exactly 1 stair per 2x2 block sector */
++-	int sy, sx;
++ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
++ 	{
++ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
++@@ -953,11 +955,7 @@ static void build_streamer2(int feat, int killwall)
++ 		/* Agent-based Drunkard's Walk */
++ 		int life = 500 + randint(1000);
++
++-		int by = y / BLOCK_HGT;
++-		int bx = x / BLOCK_WID;
++-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
++-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
++-		int current_sector = cave_sector[by][bx];
+++		int current_sector = cave_sector[y][x];
++
++ 		int steps_between_blobs = rand_range(2, 10);
++ 		int blob_length_remaining = 0;
++@@ -1004,13 +1002,10 @@ static void build_streamer2(int feat, int killwall)
++ 							}
++
++ 							/* Streams cannot cut through True Dark Mazes */
++-							if (valid)
+++						if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
++ 							{
++-								int s_y = cy / BLOCK_HGT;
++-								int s_x = cx / BLOCK_WID;
++-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
+++							valid = FALSE;
++ 								{
++-									valid = FALSE;
++ 								}
++ 							}
++
++@@ -1036,11 +1031,7 @@ static void build_streamer2(int feat, int killwall)
++
++ 			if (in_bounds(ny, nx))
++ 			{
++-				int nby = ny / BLOCK_HGT;
++-				int nbx = nx / BLOCK_WID;
++-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
++-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
++-				int next_sector = cave_sector[nby][nbx];
+++				int next_sector = cave_sector[ny][nx];
++
++ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
++ 				if (next_sector != current_sector)
++@@ -4395,12 +4386,15 @@ static void terrain_gen(void) {
++   Rand_quick = TRUE;
++   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
++
++-  /* Clear the rest of the level. */
++-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
++-    for (x = 2; x < DUNGEON_WID - 2; x++) {
+++	/* Clear the rest of the level and sector map. */
+++	for (y = 0; y < DUNGEON_HGT; y++) {
+++		for (x = 0; x < DUNGEON_WID; x++) {
+++			cave_sector[y][x] = SECTOR_RUINS;
+++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
++
++       /* Create level background */
++       cave_feat[y][x] = level_bg;
+++			}
++     }
++   }
++
++@@ -5523,58 +5517,78 @@ static void build_sector_shifting_maze(int y0, int x0)
++     }
++ }
++
+++/*
+++ * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+++ */
+++/*
+++ * Recursive helper to carve the Dark Maze
+++ */
+++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
+++{
+++    int i, d;
+++    int dirs[4] = { 0, 1, 2, 3 };
+++
+++    /* Shuffle directions */
+++    for (i = 0; i < 4; i++)
+++    {
+++        int j = rand_int(4);
+++        int temp = dirs[i];
+++        dirs[i] = dirs[j];
+++        dirs[j] = temp;
+++    }
+++
+++    for (i = 0; i < 4; i++)
+++    {
+++        int dy = 0, dx = 0;
+++        d = dirs[i];
+++        if (d == 0) dy = -2;
+++        else if (d == 1) dy = 2;
+++        else if (d == 2) dx = -2;
+++        else if (d == 3) dx = 2;
+++
+++        int ny = y + dy;
+++        int nx = x + dx;
+++
+++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
+++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
+++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
+++            cave_sector[ny][nx] == SECTOR_DARK)
+++        {
+++            cave_feat[ny][nx] = FEAT_FLOOR;
+++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
+++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
+++        }
+++    }
+++}
+++
++ /*
++  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
++  */
++ static void build_sector_dark(int y0, int x0)
++ {
++-    int y, x, i;
+++    int y, x;
++     int y1 = y0 * BLOCK_HGT;
++     int x1 = x0 * BLOCK_WID;
++     int y2 = (y0 + 2) * BLOCK_HGT - 1;
++     int x2 = (x0 + 2) * BLOCK_WID - 1;
++
++-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
+++    /* 1. Fill area with Walls and 'Darkness' flags */
++     for (y = y1; y <= y2; y++) {
++         for (x = x1; x <= x2; x++) {
++             if (!in_bounds(y, x)) continue;
++-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
+++
+++            /* Overwrite EVERYTHING to ensure a clean maze */
++             cave_feat[y][x] = FEAT_WALL_INNER;
++-            /* Outer boundary walls are diggable */
++-            if (y == y1 || y == y2 || x == x1 || x == x2) {
++-                cave_feat[y][x] = FEAT_MAGMA;
++-            }
++             cave_info[y][x] |= (CAVE_ROOM);
++             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
+++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
++         }
++     }
++
++-    /* 2. Carve Maze using square universal stamps of floor */
++-    stamp_type floor_stamp;
++-    floor_stamp.feat = FEAT_FLOOR;
++-    floor_stamp.elev = ELEV_GROUND;
++-    floor_stamp.radius = 2;
++-    floor_stamp.threshold = 90; /* high chance to stamp floor */
++-    floor_stamp.roughness = 20;
++-    floor_stamp.overwrite = TRUE;
++-    floor_stamp.circular = FALSE; /* Square brush */
++-
++-    int num_stamps = rand_range(15, 25);
++-    for (i = 0; i < num_stamps; i++) {
++-        int ty = rand_range(y1 + 2, y2 - 2);
++-        int tx = rand_range(x1 + 2, x2 - 2);
++-        universal_stamp(ty, tx, floor_stamp);
++-    }
++-
++-    /* Ensure outer walls remain solid except at access points */
++-    for (y = y1; y <= y2; y++) {
++-        for (x = x1; x <= x2; x++) {
++-            if (!in_bounds(y, x)) continue;
++-            if (y == y1 || y == y2 || x == x1 || x == x2) {
++-                cave_feat[y][x] = FEAT_MAGMA;
++-            }
++-        }
++-    }
+++    /* 2. Carve Maze using recursive backtracker */
+++    /* Starting point: y1+1, x1+1 */
+++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
+++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
++
++     /* 3. Add Monsters and Items in stamped areas */
++     for (y = y1 + 1; y < y2; y++) {
++@@ -5668,7 +5682,7 @@ static void build_sector_dark(int y0, int x0)
++         /* Open one more on the opposite side to make a passage if possible */
++         if (top_opened || left_opened) {
++             cave_feat[y2][x2-1] = FEAT_FLOOR;
++-        } else {
+++        } else if (bottom_opened || right_opened) {
++             cave_feat[y1][x1+1] = FEAT_FLOOR;
++         }
++     }
++@@ -5951,21 +5965,13 @@ static void jitter_sector_boundaries(void)
++ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
++
++ 			bool is_boundary = FALSE;
++-			int by = y / BLOCK_HGT;
++-			int bx = x / BLOCK_WID;
++-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
++-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
++-			int sect = cave_sector[by][bx];
++-
++-			int by_minus = (y - 1) / BLOCK_HGT;
++-			int bx_minus = (x - 1) / BLOCK_WID;
++-			int by_plus = (y + 1) / BLOCK_HGT;
++-			int bx_plus = (x + 1) / BLOCK_WID;
++-
++-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
++-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
++-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
++-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
+++			int sect = cave_sector[y][x];
+++
+++
+++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
+++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
+++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
+++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
++
++ 			if (is_boundary)
++ 			{
++@@ -6040,12 +6046,9 @@ static void smooth_caverns(void)
++ 			for (x = 1; x < DUNGEON_WID - 1; x++)
++ 			{
++ 				int by = y / BLOCK_HGT;
++-				int bx = x / BLOCK_WID;
++-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
++-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
++
++ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
++-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
+++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
++
++ 				/* Do not overwrite permanent features or stairs/doors */
++ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
++@@ -6209,7 +6212,7 @@ static void cave_gen(void)
++ 	/* Initialize Elevation */
++ 	init_elevation();
++
++-	/* Initialize Sector Map to RUINS by default */
+++	/* Initialize Sector Map to RUINS by default for all blocks */
++ 	for (y = 0; y < dun->row_rooms; y++)
++ 	{
++ 		for (x = 0; x < dun->col_rooms; x++)
++@@ -6331,7 +6334,7 @@ static void cave_gen(void)
++ 		if (room_build(y, x, 1))
++ 		{
++ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
++-			if (cave_sector[y][x] == SECTOR_RUINS)
+++			if (dun->sector_map[y][x] == SECTOR_RUINS)
++ 			{
++ 				int cy, cx;
++ 				for (cy = y - 1; cy <= y + 1; cy++)
++@@ -6376,17 +6379,26 @@ static void cave_gen(void)
++ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
++ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
++
++-			/* Assign to 2x2 block */
++-			cave_sector[y][x] = sect_type;
++-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
++-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
++-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
+++			/* Assign to all blocks in the 2x2 sector group */
+++			dun->sector_map[y][x] = sect_type;
+++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
+++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
+++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
++ 		}
++ 	}
++
++ 	/* No "crowded" rooms yet */
++ 	dun->crowded = FALSE;
++
+++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
+++	for (y = 0; y < DUNGEON_HGT; y++)
+++	{
+++		for (x = 0; x < DUNGEON_WID; x++)
+++		{
+++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
+++		}
+++	}
+++
++
++ 	/* No rooms yet */
++ 	dun->cent_n = 0;
++@@ -6396,7 +6408,7 @@ static void cave_gen(void)
++ 	{
++ 		for (x = 0; x < dun->col_rooms; x += 2)
++ 		{
++-			int sect = cave_sector[y][x];
+++			int sect = dun->sector_map[y][x];
++ 			if (sect == SECTOR_SHIFTING)
++ 				{
++ 					build_sector_shifting_maze(y, x);
++@@ -6643,16 +6655,6 @@ static void cave_gen(void)
++ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
++ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
++
++-		for (sy = 0; sy < DUNGEON_HGT; sy++) {
++-			for (sx = 0; sx < DUNGEON_WID; sx++) {
++-				if (cave_feat[sy][sx] == start_feat) {
++-					set_generation_origin(sy, sx);
++-					found_start = TRUE;
++-					break;
++-				}
++-			}
++-			if (found_start) break;
++-		}
++ 		if (!found_start) {
++ 			/* Fallback to center */
++ 			set_generation_origin(DUNGEON_HGT / 2, DUNGEON_WID / 2);
++@@ -6792,6 +6794,7 @@ static void cave_gen(void)
++ 	}
++
++ 	free(dun_body);
+++	dun = NULL;
++ }
++
++
+diff --git a/kamband_stats.csv b/kamband_stats.csv
+new file mode 100644
+index 0000000..dda3f10
+--- /dev/null
++++ b/kamband_stats.csv
+@@ -0,0 +1,2 @@
++turn,event_type,duration_ms,mem_kb,depth,x,y,elevation
++1,level_gen,6911,10496,29,365,58,0
+diff --git a/lib/data/a_info.raw b/lib/data/a_info.raw
+new file mode 100644
+index 0000000..97245a0
+Binary files /dev/null and b/lib/data/a_info.raw differ
+diff --git a/lib/data/e_info.raw b/lib/data/e_info.raw
+new file mode 100644
+index 0000000..1323fe7
+Binary files /dev/null and b/lib/data/e_info.raw differ
+diff --git a/lib/data/f_info.raw b/lib/data/f_info.raw
+new file mode 100644
+index 0000000..6bc9a4f
+Binary files /dev/null and b/lib/data/f_info.raw differ
+diff --git a/lib/data/k_info.raw b/lib/data/k_info.raw
+new file mode 100644
+index 0000000..f4bc9c7
+Binary files /dev/null and b/lib/data/k_info.raw differ
+diff --git a/lib/data/r_info.raw b/lib/data/r_info.raw
+new file mode 100644
+index 0000000..0b7ee1b
+Binary files /dev/null and b/lib/data/r_info.raw differ
+diff --git a/lib/data/v_info.raw b/lib/data/v_info.raw
+new file mode 100644
+index 0000000..f3abcc6
+Binary files /dev/null and b/lib/data/v_info.raw differ
+diff --git a/lib/save/1001.Jules b/lib/save/1001.Jules
+new file mode 100644
+index 0000000..626d5ca
+Binary files /dev/null and b/lib/save/1001.Jules differ
+diff --git a/load_debug.log b/load_debug.log
+new file mode 100644
+index 0000000..a5effe7
+--- /dev/null
++++ b/load_debug.log
+@@ -0,0 +1,13 @@
++rd_savefile_new: start
++Loading a 2.1.9 savefile...
++rd_extra: start
++rd_extra: seeds read
++rd_extra: death flag read
++Loaded Turn: 1
++Starting to restore Dungeon...
++Restoring Dungeon...
++Dungeon Header: Depth 29, PY 58, PX 365, YMAX 440, XMAX 462
++rd_dungeon: start RLE 1 (info)
++rd_dungeon: start RLE 2 (fire life)
++rd_dungeon: start RLE 3 (feat)
++rd_dungeon: start RLE 4 (sector)
+diff --git a/patch.diff b/patch.diff
+index a04d6b9..84cd1d0 100644
+--- a/patch.diff
++++ b/patch.diff
+@@ -1,61 +1,344 @@
+-<<<<<<< SEARCH
+-/*
+- * Swap the players/monsters (if any) at two locations XXX XXX XXX
+- */
+-void monster_swap(int y1, int x1, int y2, int x2)
+-=======
+-/* Process terrain effects for a monster at (y, x) */
+-void mon_process_terrain(int m_idx, int y, int x)
+-{
+-	monster_type *m_ptr = &m_list[m_idx];
+-	monster_race *r_ptr = &r_info[m_ptr->r_idx];
+-	int feat = cave_feat[y][x];
+-
+-	/* Flying monsters ignore ground hazards */
+-	if (r_ptr->flags2 & RF2_FLY) return;
+-
+-	/* Handle Oil */
+-	if (feat == FEAT_OIL)
+-	{
+-		/* Energy penalty: slower movement */
+-		m_ptr->energy -= 50;
+-
+-		/* Slip check: 50% chance (increased for insects) */
+-		int chance = 50;
+-		if (strchr("aAsS", r_ptr->d_char)) chance = 80; /* Ants/Spiders hate oil */
+-
+-		if (rand_int(100) < chance)
+-		{
+-			if (m_ptr->ml && m_ptr->is_pet)
+-			{
+-				char m_name[80];
+-				monster_desc(m_name, m_ptr, 0);
+-				msg_format("The %s slips on the oil!", m_name);
+-			}
+-			/* 'Knock out' - heavy stun or temporary sleep */
+-			m_ptr->stunned += 10 + rand_int(20);
+-			if (m_ptr->stunned > 50) m_ptr->csleep = 5; /* Actually knocked out */
+-		}
+-	}
+-
+-	/* Handle Ice */
+-	if (feat == FEAT_ICE)
+-	{
+-		if (rand_int(100) < 30 && !(r_ptr->flags3 & RF3_IM_COLD))
+-		{
+-			if (m_ptr->ml && m_ptr->is_pet)
+-			{
+-				char m_name[80];
+-				monster_desc(m_name, m_ptr, 0);
+-				msg_format("The %s loses its footing on the ice!", m_name);
+-			}
+-			m_ptr->stunned += rand_int(10);
+-		}
+-	}
+-}
+-
+-/*
+- * Swap the players/monsters (if any) at two locations XXX XXX XXX
+- */
+-void monster_swap(int y1, int x1, int y2, int x2)
+->>>>>>> REPLACE
++diff --git a/src/generate.c b/src/generate.c
++index 17ac01d..3dbb878 100644
++--- a/src/generate.c
+++++ b/src/generate.c
++@@ -315,6 +315,9 @@ struct dun_data
++ 	/* Array of which blocks are used */
++ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+++	/* Array of sector types for each block */
+++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+++
++ 	/* Hack -- there is a pit/nest on this level */
++ 	bool crowded;
++ };
++@@ -707,9 +710,9 @@ static void place_random_door(int y, int x)
++  */
++ static void alloc_stairs(int feat, int num, int walls, bool force_room)
++ {
++-	int y, x, j;
+++	int y, x, j, sy, sx;
++
++-	/* The walls and num parameters are unused */
+++	/* The walls and num parameters are mostly unused in the sector-based version */
++ 	(void)walls;
++ 	(void)num;
++
++@@ -734,7 +737,6 @@ static void alloc_stairs(int feat, int num, int walls, bool force_room)
++     }
++
++ 	/* Allocate exactly 1 stair per 2x2 block sector */
++-	int sy, sx;
++ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
++ 	{
++ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
++@@ -953,11 +955,7 @@ static void build_streamer2(int feat, int killwall)
++ 		/* Agent-based Drunkard's Walk */
++ 		int life = 500 + randint(1000);
++
++-		int by = y / BLOCK_HGT;
++-		int bx = x / BLOCK_WID;
++-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
++-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
++-		int current_sector = cave_sector[by][bx];
+++		int current_sector = cave_sector[y][x];
++
++ 		int steps_between_blobs = rand_range(2, 10);
++ 		int blob_length_remaining = 0;
++@@ -1004,13 +1002,10 @@ static void build_streamer2(int feat, int killwall)
++ 							}
++
++ 							/* Streams cannot cut through True Dark Mazes */
++-							if (valid)
+++						if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
++ 							{
++-								int s_y = cy / BLOCK_HGT;
++-								int s_x = cx / BLOCK_WID;
++-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
+++							valid = FALSE;
++ 								{
++-									valid = FALSE;
++ 								}
++ 							}
++
++@@ -1036,11 +1031,7 @@ static void build_streamer2(int feat, int killwall)
++
++ 			if (in_bounds(ny, nx))
++ 			{
++-				int nby = ny / BLOCK_HGT;
++-				int nbx = nx / BLOCK_WID;
++-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
++-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
++-				int next_sector = cave_sector[nby][nbx];
+++				int next_sector = cave_sector[ny][nx];
++
++ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
++ 				if (next_sector != current_sector)
++@@ -4395,12 +4386,15 @@ static void terrain_gen(void) {
++   Rand_quick = TRUE;
++   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
++
++-  /* Clear the rest of the level. */
++-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
++-    for (x = 2; x < DUNGEON_WID - 2; x++) {
+++	/* Clear the rest of the level and sector map. */
+++	for (y = 0; y < DUNGEON_HGT; y++) {
+++		for (x = 0; x < DUNGEON_WID; x++) {
+++			cave_sector[y][x] = SECTOR_RUINS;
+++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
++
++       /* Create level background */
++       cave_feat[y][x] = level_bg;
+++			}
++     }
++   }
++
++@@ -5523,58 +5517,78 @@ static void build_sector_shifting_maze(int y0, int x0)
++     }
++ }
++
+++/*
+++ * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+++ */
+++/*
+++ * Recursive helper to carve the Dark Maze
+++ */
+++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
+++{
+++    int i, d;
+++    int dirs[4] = { 0, 1, 2, 3 };
+++
+++    /* Shuffle directions */
+++    for (i = 0; i < 4; i++)
+++    {
+++        int j = rand_int(4);
+++        int temp = dirs[i];
+++        dirs[i] = dirs[j];
+++        dirs[j] = temp;
+++    }
+++
+++    for (i = 0; i < 4; i++)
+++    {
+++        int dy = 0, dx = 0;
+++        d = dirs[i];
+++        if (d == 0) dy = -2;
+++        else if (d == 1) dy = 2;
+++        else if (d == 2) dx = -2;
+++        else if (d == 3) dx = 2;
+++
+++        int ny = y + dy;
+++        int nx = x + dx;
+++
+++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
+++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
+++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
+++            cave_sector[ny][nx] == SECTOR_DARK)
+++        {
+++            cave_feat[ny][nx] = FEAT_FLOOR;
+++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
+++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
+++        }
+++    }
+++}
+++
++ /*
++  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
++  */
++ static void build_sector_dark(int y0, int x0)
++ {
++-    int y, x, i;
+++    int y, x;
++     int y1 = y0 * BLOCK_HGT;
++     int x1 = x0 * BLOCK_WID;
++     int y2 = (y0 + 2) * BLOCK_HGT - 1;
++     int x2 = (x0 + 2) * BLOCK_WID - 1;
++
++-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
+++    /* 1. Fill area with Walls and 'Darkness' flags */
++     for (y = y1; y <= y2; y++) {
++         for (x = x1; x <= x2; x++) {
++             if (!in_bounds(y, x)) continue;
++-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
+++
+++            /* Overwrite EVERYTHING to ensure a clean maze */
++             cave_feat[y][x] = FEAT_WALL_INNER;
++-            /* Outer boundary walls are diggable */
++-            if (y == y1 || y == y2 || x == x1 || x == x2) {
++-                cave_feat[y][x] = FEAT_MAGMA;
++-            }
++             cave_info[y][x] |= (CAVE_ROOM);
++             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
+++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
++         }
++     }
++
++-    /* 2. Carve Maze using square universal stamps of floor */
++-    stamp_type floor_stamp;
++-    floor_stamp.feat = FEAT_FLOOR;
++-    floor_stamp.elev = ELEV_GROUND;
++-    floor_stamp.radius = 2;
++-    floor_stamp.threshold = 90; /* high chance to stamp floor */
++-    floor_stamp.roughness = 20;
++-    floor_stamp.overwrite = TRUE;
++-    floor_stamp.circular = FALSE; /* Square brush */
++-
++-    int num_stamps = rand_range(15, 25);
++-    for (i = 0; i < num_stamps; i++) {
++-        int ty = rand_range(y1 + 2, y2 - 2);
++-        int tx = rand_range(x1 + 2, x2 - 2);
++-        universal_stamp(ty, tx, floor_stamp);
++-    }
++-
++-    /* Ensure outer walls remain solid except at access points */
++-    for (y = y1; y <= y2; y++) {
++-        for (x = x1; x <= x2; x++) {
++-            if (!in_bounds(y, x)) continue;
++-            if (y == y1 || y == y2 || x == x1 || x == x2) {
++-                cave_feat[y][x] = FEAT_MAGMA;
++-            }
++-        }
++-    }
+++    /* 2. Carve Maze using recursive backtracker */
+++    /* Starting point: y1+1, x1+1 */
+++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
+++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
++
++     /* 3. Add Monsters and Items in stamped areas */
++     for (y = y1 + 1; y < y2; y++) {
++@@ -5668,7 +5682,7 @@ static void build_sector_dark(int y0, int x0)
++         /* Open one more on the opposite side to make a passage if possible */
++         if (top_opened || left_opened) {
++             cave_feat[y2][x2-1] = FEAT_FLOOR;
++-        } else {
+++        } else if (bottom_opened || right_opened) {
++             cave_feat[y1][x1+1] = FEAT_FLOOR;
++         }
++     }
++@@ -5951,21 +5965,13 @@ static void jitter_sector_boundaries(void)
++ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
++
++ 			bool is_boundary = FALSE;
++-			int by = y / BLOCK_HGT;
++-			int bx = x / BLOCK_WID;
++-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
++-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
++-			int sect = cave_sector[by][bx];
++-
++-			int by_minus = (y - 1) / BLOCK_HGT;
++-			int bx_minus = (x - 1) / BLOCK_WID;
++-			int by_plus = (y + 1) / BLOCK_HGT;
++-			int bx_plus = (x + 1) / BLOCK_WID;
++-
++-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
++-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
++-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
++-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
+++			int sect = cave_sector[y][x];
+++
+++
+++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
+++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
+++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
+++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
++
++ 			if (is_boundary)
++ 			{
++@@ -6040,12 +6046,9 @@ static void smooth_caverns(void)
++ 			for (x = 1; x < DUNGEON_WID - 1; x++)
++ 			{
++ 				int by = y / BLOCK_HGT;
++-				int bx = x / BLOCK_WID;
++-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
++-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
++
++ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
++-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
+++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
++
++ 				/* Do not overwrite permanent features or stairs/doors */
++ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
++@@ -6209,7 +6212,7 @@ static void cave_gen(void)
++ 	/* Initialize Elevation */
++ 	init_elevation();
++
++-	/* Initialize Sector Map to RUINS by default */
+++	/* Initialize Sector Map to RUINS by default for all blocks */
++ 	for (y = 0; y < dun->row_rooms; y++)
++ 	{
++ 		for (x = 0; x < dun->col_rooms; x++)
++@@ -6331,7 +6334,7 @@ static void cave_gen(void)
++ 		if (room_build(y, x, 1))
++ 		{
++ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
++-			if (cave_sector[y][x] == SECTOR_RUINS)
+++			if (dun->sector_map[y][x] == SECTOR_RUINS)
++ 			{
++ 				int cy, cx;
++ 				for (cy = y - 1; cy <= y + 1; cy++)
++@@ -6376,17 +6379,26 @@ static void cave_gen(void)
++ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
++ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
++
++-			/* Assign to 2x2 block */
++-			cave_sector[y][x] = sect_type;
++-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
++-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
++-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
+++			/* Assign to all blocks in the 2x2 sector group */
+++			dun->sector_map[y][x] = sect_type;
+++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
+++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
+++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
++ 		}
++ 	}
++
++ 	/* No "crowded" rooms yet */
++ 	dun->crowded = FALSE;
++
+++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
+++	for (y = 0; y < DUNGEON_HGT; y++)
+++	{
+++		for (x = 0; x < DUNGEON_WID; x++)
+++		{
+++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
+++		}
+++	}
+++
++
++ 	/* No rooms yet */
++ 	dun->cent_n = 0;
++@@ -6396,7 +6408,7 @@ static void cave_gen(void)
++ 	{
++ 		for (x = 0; x < dun->col_rooms; x += 2)
++ 		{
++-			int sect = cave_sector[y][x];
+++			int sect = dun->sector_map[y][x];
++ 			if (sect == SECTOR_SHIFTING)
++ 				{
++ 					build_sector_shifting_maze(y, x);
++@@ -6643,17 +6655,6 @@ static void cave_gen(void)
++ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
++ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
++
++-		for (sy = 0; sy < DUNGEON_HGT; sy++) {
++-			for (sx = 0; sx < DUNGEON_WID; sx++) {
++-				if (cave_feat[sy][sx] == start_feat) {
++-					set_generation_origin(sy, sx);
++-					found_start = TRUE;
++-					break;
++-				}
++-			}
++-			if (found_start) break;
++-		}
++-		if (!found_start) {
++ 			/* Fallback to center */
++ 			set_generation_origin(DUNGEON_HGT / 2, DUNGEON_WID / 2);
++ 		}
++@@ -6792,6 +6793,7 @@ static void cave_gen(void)
++ 	}
++
++ 	free(dun_body);
+++	dun = NULL;
++ }
++
++
+diff --git a/src/generate.c b/src/generate.c
+index 17ac01d..3dbb878 100644
+--- a/src/generate.c
++++ b/src/generate.c
+@@ -315,6 +315,9 @@ struct dun_data
+ 	/* Array of which blocks are used */
+ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+
++	/* Array of sector types for each block */
++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+ 	/* Hack -- there is a pit/nest on this level */
+ 	bool crowded;
+ };
+@@ -707,9 +710,9 @@ static void place_random_door(int y, int x)
+  */
+ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+ {
+-	int y, x, j;
++	int y, x, j, sy, sx;
+
+-	/* The walls and num parameters are unused */
++	/* The walls and num parameters are mostly unused in the sector-based version */
+ 	(void)walls;
+ 	(void)num;
+
+@@ -734,7 +737,6 @@ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+     }
+
+ 	/* Allocate exactly 1 stair per 2x2 block sector */
+-	int sy, sx;
+ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
+ 	{
+ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
+@@ -953,11 +955,7 @@ static void build_streamer2(int feat, int killwall)
+ 		/* Agent-based Drunkard's Walk */
+ 		int life = 500 + randint(1000);
+
+-		int by = y / BLOCK_HGT;
+-		int bx = x / BLOCK_WID;
+-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-		int current_sector = cave_sector[by][bx];
++		int current_sector = cave_sector[y][x];
+
+ 		int steps_between_blobs = rand_range(2, 10);
+ 		int blob_length_remaining = 0;
+@@ -1004,13 +1002,10 @@ static void build_streamer2(int feat, int killwall)
+ 							}
+
+ 							/* Streams cannot cut through True Dark Mazes */
+-							if (valid)
++						if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
+ 							{
+-								int s_y = cy / BLOCK_HGT;
+-								int s_x = cx / BLOCK_WID;
+-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
++							valid = FALSE;
+ 								{
+-									valid = FALSE;
+ 								}
+ 							}
+
+@@ -1036,11 +1031,7 @@ static void build_streamer2(int feat, int killwall)
+
+ 			if (in_bounds(ny, nx))
+ 			{
+-				int nby = ny / BLOCK_HGT;
+-				int nbx = nx / BLOCK_WID;
+-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
+-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
+-				int next_sector = cave_sector[nby][nbx];
++				int next_sector = cave_sector[ny][nx];
+
+ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
+ 				if (next_sector != current_sector)
+@@ -4395,12 +4386,15 @@ static void terrain_gen(void) {
+   Rand_quick = TRUE;
+   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
+
+-  /* Clear the rest of the level. */
+-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
+-    for (x = 2; x < DUNGEON_WID - 2; x++) {
++	/* Clear the rest of the level and sector map. */
++	for (y = 0; y < DUNGEON_HGT; y++) {
++		for (x = 0; x < DUNGEON_WID; x++) {
++			cave_sector[y][x] = SECTOR_RUINS;
++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
+
+       /* Create level background */
+       cave_feat[y][x] = level_bg;
++			}
+     }
+   }
+
+@@ -5523,58 +5517,78 @@ static void build_sector_shifting_maze(int y0, int x0)
+     }
+ }
+
++/*
++ * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
++ */
++/*
++ * Recursive helper to carve the Dark Maze
++ */
++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
++{
++    int i, d;
++    int dirs[4] = { 0, 1, 2, 3 };
++
++    /* Shuffle directions */
++    for (i = 0; i < 4; i++)
++    {
++        int j = rand_int(4);
++        int temp = dirs[i];
++        dirs[i] = dirs[j];
++        dirs[j] = temp;
++    }
++
++    for (i = 0; i < 4; i++)
++    {
++        int dy = 0, dx = 0;
++        d = dirs[i];
++        if (d == 0) dy = -2;
++        else if (d == 1) dy = 2;
++        else if (d == 2) dx = -2;
++        else if (d == 3) dx = 2;
++
++        int ny = y + dy;
++        int nx = x + dx;
++
++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
++            cave_sector[ny][nx] == SECTOR_DARK)
++        {
++            cave_feat[ny][nx] = FEAT_FLOOR;
++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
++        }
++    }
++}
++
+ /*
+  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+  */
+ static void build_sector_dark(int y0, int x0)
+ {
+-    int y, x, i;
++    int y, x;
+     int y1 = y0 * BLOCK_HGT;
+     int x1 = x0 * BLOCK_WID;
+     int y2 = (y0 + 2) * BLOCK_HGT - 1;
+     int x2 = (x0 + 2) * BLOCK_WID - 1;
+
+-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
++    /* 1. Fill area with Walls and 'Darkness' flags */
+     for (y = y1; y <= y2; y++) {
+         for (x = x1; x <= x2; x++) {
+             if (!in_bounds(y, x)) continue;
+-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
++
++            /* Overwrite EVERYTHING to ensure a clean maze */
+             cave_feat[y][x] = FEAT_WALL_INNER;
+-            /* Outer boundary walls are diggable */
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+             cave_info[y][x] |= (CAVE_ROOM);
+             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
+         }
+     }
+
+-    /* 2. Carve Maze using square universal stamps of floor */
+-    stamp_type floor_stamp;
+-    floor_stamp.feat = FEAT_FLOOR;
+-    floor_stamp.elev = ELEV_GROUND;
+-    floor_stamp.radius = 2;
+-    floor_stamp.threshold = 90; /* high chance to stamp floor */
+-    floor_stamp.roughness = 20;
+-    floor_stamp.overwrite = TRUE;
+-    floor_stamp.circular = FALSE; /* Square brush */
+-
+-    int num_stamps = rand_range(15, 25);
+-    for (i = 0; i < num_stamps; i++) {
+-        int ty = rand_range(y1 + 2, y2 - 2);
+-        int tx = rand_range(x1 + 2, x2 - 2);
+-        universal_stamp(ty, tx, floor_stamp);
+-    }
+-
+-    /* Ensure outer walls remain solid except at access points */
+-    for (y = y1; y <= y2; y++) {
+-        for (x = x1; x <= x2; x++) {
+-            if (!in_bounds(y, x)) continue;
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+-        }
+-    }
++    /* 2. Carve Maze using recursive backtracker */
++    /* Starting point: y1+1, x1+1 */
++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
+
+     /* 3. Add Monsters and Items in stamped areas */
+     for (y = y1 + 1; y < y2; y++) {
+@@ -5668,7 +5682,7 @@ static void build_sector_dark(int y0, int x0)
+         /* Open one more on the opposite side to make a passage if possible */
+         if (top_opened || left_opened) {
+             cave_feat[y2][x2-1] = FEAT_FLOOR;
+-        } else {
++        } else if (bottom_opened || right_opened) {
+             cave_feat[y1][x1+1] = FEAT_FLOOR;
+         }
+     }
+@@ -5951,21 +5965,13 @@ static void jitter_sector_boundaries(void)
+ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
+
+ 			bool is_boundary = FALSE;
+-			int by = y / BLOCK_HGT;
+-			int bx = x / BLOCK_WID;
+-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-			int sect = cave_sector[by][bx];
+-
+-			int by_minus = (y - 1) / BLOCK_HGT;
+-			int bx_minus = (x - 1) / BLOCK_WID;
+-			int by_plus = (y + 1) / BLOCK_HGT;
+-			int bx_plus = (x + 1) / BLOCK_WID;
+-
+-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
+-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
+-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
+-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
++			int sect = cave_sector[y][x];
++
++
++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
+
+ 			if (is_boundary)
+ 			{
+@@ -6040,12 +6046,9 @@ static void smooth_caverns(void)
+ 			for (x = 1; x < DUNGEON_WID - 1; x++)
+ 			{
+ 				int by = y / BLOCK_HGT;
+-				int bx = x / BLOCK_WID;
+-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+
+ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
+-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
+
+ 				/* Do not overwrite permanent features or stairs/doors */
+ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
+@@ -6209,7 +6212,7 @@ static void cave_gen(void)
+ 	/* Initialize Elevation */
+ 	init_elevation();
+
+-	/* Initialize Sector Map to RUINS by default */
++	/* Initialize Sector Map to RUINS by default for all blocks */
+ 	for (y = 0; y < dun->row_rooms; y++)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x++)
+@@ -6331,7 +6334,7 @@ static void cave_gen(void)
+ 		if (room_build(y, x, 1))
+ 		{
+ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
+-			if (cave_sector[y][x] == SECTOR_RUINS)
++			if (dun->sector_map[y][x] == SECTOR_RUINS)
+ 			{
+ 				int cy, cx;
+ 				for (cy = y - 1; cy <= y + 1; cy++)
+@@ -6376,17 +6379,26 @@ static void cave_gen(void)
+ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
+ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
+
+-			/* Assign to 2x2 block */
+-			cave_sector[y][x] = sect_type;
+-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
+-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
+-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
++			/* Assign to all blocks in the 2x2 sector group */
++			dun->sector_map[y][x] = sect_type;
++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
+ 		}
+ 	}
+
+ 	/* No "crowded" rooms yet */
+ 	dun->crowded = FALSE;
+
++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
++	for (y = 0; y < DUNGEON_HGT; y++)
++	{
++		for (x = 0; x < DUNGEON_WID; x++)
++		{
++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
++		}
++	}
++
+
+ 	/* No rooms yet */
+ 	dun->cent_n = 0;
+@@ -6396,7 +6408,7 @@ static void cave_gen(void)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x += 2)
+ 		{
+-			int sect = cave_sector[y][x];
++			int sect = dun->sector_map[y][x];
+ 			if (sect == SECTOR_SHIFTING)
+ 				{
+ 					build_sector_shifting_maze(y, x);
+@@ -6643,17 +6655,6 @@ static void cave_gen(void)
+ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
+ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
+
+-		for (sy = 0; sy < DUNGEON_HGT; sy++) {
+-			for (sx = 0; sx < DUNGEON_WID; sx++) {
+-				if (cave_feat[sy][sx] == start_feat) {
+-					set_generation_origin(sy, sx);
+-					found_start = TRUE;
+-					break;
+-				}
+-			}
+-			if (found_start) break;
+-		}
+-		if (!found_start) {
+ 			/* Fallback to center */
+ 			set_generation_origin(DUNGEON_HGT / 2, DUNGEON_WID / 2);
+ 		}
+@@ -6792,6 +6793,7 @@ static void cave_gen(void)
+ 	}
+
+ 	free(dun_body);
++	dun = NULL;
+ }
+
+
diff --git a/diff_out b/diff_out
new file mode 100644
index 0000000..e1192bc
--- /dev/null
+++ b/diff_out
@@ -0,0 +1,398 @@
+diff --git a/kamband_stats.csv b/kamband_stats.csv
+new file mode 100644
+index 0000000..dda3f10
+--- /dev/null
++++ b/kamband_stats.csv
+@@ -0,0 +1,2 @@
++turn,event_type,duration_ms,mem_kb,depth,x,y,elevation
++1,level_gen,6911,10496,29,365,58,0
+diff --git a/lib/data/a_info.raw b/lib/data/a_info.raw
+new file mode 100644
+index 0000000..97245a0
+Binary files /dev/null and b/lib/data/a_info.raw differ
+diff --git a/lib/data/e_info.raw b/lib/data/e_info.raw
+new file mode 100644
+index 0000000..1323fe7
+Binary files /dev/null and b/lib/data/e_info.raw differ
+diff --git a/lib/data/f_info.raw b/lib/data/f_info.raw
+new file mode 100644
+index 0000000..6bc9a4f
+Binary files /dev/null and b/lib/data/f_info.raw differ
+diff --git a/lib/data/k_info.raw b/lib/data/k_info.raw
+new file mode 100644
+index 0000000..f4bc9c7
+Binary files /dev/null and b/lib/data/k_info.raw differ
+diff --git a/lib/data/r_info.raw b/lib/data/r_info.raw
+new file mode 100644
+index 0000000..0b7ee1b
+Binary files /dev/null and b/lib/data/r_info.raw differ
+diff --git a/lib/data/v_info.raw b/lib/data/v_info.raw
+new file mode 100644
+index 0000000..f3abcc6
+Binary files /dev/null and b/lib/data/v_info.raw differ
+diff --git a/lib/save/1001.Jules b/lib/save/1001.Jules
+new file mode 100644
+index 0000000..626d5ca
+Binary files /dev/null and b/lib/save/1001.Jules differ
+diff --git a/load_debug.log b/load_debug.log
+new file mode 100644
+index 0000000..a5effe7
+--- /dev/null
++++ b/load_debug.log
+@@ -0,0 +1,13 @@
++rd_savefile_new: start
++Loading a 2.1.9 savefile...
++rd_extra: start
++rd_extra: seeds read
++rd_extra: death flag read
++Loaded Turn: 1
++Starting to restore Dungeon...
++Restoring Dungeon...
++Dungeon Header: Depth 29, PY 58, PX 365, YMAX 440, XMAX 462
++rd_dungeon: start RLE 1 (info)
++rd_dungeon: start RLE 2 (fire life)
++rd_dungeon: start RLE 3 (feat)
++rd_dungeon: start RLE 4 (sector)
+diff --git a/src/generate.c b/src/generate.c
+index 17ac01d..5187da5 100644
+--- a/src/generate.c
++++ b/src/generate.c
+@@ -315,6 +315,9 @@ struct dun_data
+ 	/* Array of which blocks are used */
+ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+
++	/* Array of sector types for each block */
++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+ 	/* Hack -- there is a pit/nest on this level */
+ 	bool crowded;
+ };
+@@ -707,9 +710,9 @@ static void place_random_door(int y, int x)
+  */
+ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+ {
+-	int y, x, j;
++	int y, x, j, sy, sx;
+
+-	/* The walls and num parameters are unused */
++	/* The walls and num parameters are mostly unused in the sector-based version */
+ 	(void)walls;
+ 	(void)num;
+
+@@ -734,7 +737,6 @@ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+     }
+
+ 	/* Allocate exactly 1 stair per 2x2 block sector */
+-	int sy, sx;
+ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
+ 	{
+ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
+@@ -953,11 +955,7 @@ static void build_streamer2(int feat, int killwall)
+ 		/* Agent-based Drunkard's Walk */
+ 		int life = 500 + randint(1000);
+
+-		int by = y / BLOCK_HGT;
+-		int bx = x / BLOCK_WID;
+-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-		int current_sector = cave_sector[by][bx];
++		int current_sector = cave_sector[y][x];
+
+ 		int steps_between_blobs = rand_range(2, 10);
+ 		int blob_length_remaining = 0;
+@@ -1004,13 +1002,10 @@ static void build_streamer2(int feat, int killwall)
+ 							}
+
+ 							/* Streams cannot cut through True Dark Mazes */
+-							if (valid)
++						if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
+ 							{
+-								int s_y = cy / BLOCK_HGT;
+-								int s_x = cx / BLOCK_WID;
+-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
++							valid = FALSE;
+ 								{
+-									valid = FALSE;
+ 								}
+ 							}
+
+@@ -1036,11 +1031,7 @@ static void build_streamer2(int feat, int killwall)
+
+ 			if (in_bounds(ny, nx))
+ 			{
+-				int nby = ny / BLOCK_HGT;
+-				int nbx = nx / BLOCK_WID;
+-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
+-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
+-				int next_sector = cave_sector[nby][nbx];
++				int next_sector = cave_sector[ny][nx];
+
+ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
+ 				if (next_sector != current_sector)
+@@ -4395,12 +4386,15 @@ static void terrain_gen(void) {
+   Rand_quick = TRUE;
+   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
+
+-  /* Clear the rest of the level. */
+-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
+-    for (x = 2; x < DUNGEON_WID - 2; x++) {
++	/* Clear the rest of the level and sector map. */
++	for (y = 0; y < DUNGEON_HGT; y++) {
++		for (x = 0; x < DUNGEON_WID; x++) {
++			cave_sector[y][x] = SECTOR_RUINS;
++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
+
+       /* Create level background */
+       cave_feat[y][x] = level_bg;
++			}
+     }
+   }
+
+@@ -5523,58 +5517,78 @@ static void build_sector_shifting_maze(int y0, int x0)
+     }
+ }
+
++/*
++ * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
++ */
++/*
++ * Recursive helper to carve the Dark Maze
++ */
++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
++{
++    int i, d;
++    int dirs[4] = { 0, 1, 2, 3 };
++
++    /* Shuffle directions */
++    for (i = 0; i < 4; i++)
++    {
++        int j = rand_int(4);
++        int temp = dirs[i];
++        dirs[i] = dirs[j];
++        dirs[j] = temp;
++    }
++
++    for (i = 0; i < 4; i++)
++    {
++        int dy = 0, dx = 0;
++        d = dirs[i];
++        if (d == 0) dy = -2;
++        else if (d == 1) dy = 2;
++        else if (d == 2) dx = -2;
++        else if (d == 3) dx = 2;
++
++        int ny = y + dy;
++        int nx = x + dx;
++
++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
++            cave_sector[ny][nx] == SECTOR_DARK)
++        {
++            cave_feat[ny][nx] = FEAT_FLOOR;
++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
++        }
++    }
++}
++
+ /*
+  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+  */
+ static void build_sector_dark(int y0, int x0)
+ {
+-    int y, x, i;
++    int y, x;
+     int y1 = y0 * BLOCK_HGT;
+     int x1 = x0 * BLOCK_WID;
+     int y2 = (y0 + 2) * BLOCK_HGT - 1;
+     int x2 = (x0 + 2) * BLOCK_WID - 1;
+
+-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
++    /* 1. Fill area with Walls and 'Darkness' flags */
+     for (y = y1; y <= y2; y++) {
+         for (x = x1; x <= x2; x++) {
+             if (!in_bounds(y, x)) continue;
+-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
++
++            /* Overwrite EVERYTHING to ensure a clean maze */
+             cave_feat[y][x] = FEAT_WALL_INNER;
+-            /* Outer boundary walls are diggable */
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+             cave_info[y][x] |= (CAVE_ROOM);
+             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
+         }
+     }
+
+-    /* 2. Carve Maze using square universal stamps of floor */
+-    stamp_type floor_stamp;
+-    floor_stamp.feat = FEAT_FLOOR;
+-    floor_stamp.elev = ELEV_GROUND;
+-    floor_stamp.radius = 2;
+-    floor_stamp.threshold = 90; /* high chance to stamp floor */
+-    floor_stamp.roughness = 20;
+-    floor_stamp.overwrite = TRUE;
+-    floor_stamp.circular = FALSE; /* Square brush */
+-
+-    int num_stamps = rand_range(15, 25);
+-    for (i = 0; i < num_stamps; i++) {
+-        int ty = rand_range(y1 + 2, y2 - 2);
+-        int tx = rand_range(x1 + 2, x2 - 2);
+-        universal_stamp(ty, tx, floor_stamp);
+-    }
+-
+-    /* Ensure outer walls remain solid except at access points */
+-    for (y = y1; y <= y2; y++) {
+-        for (x = x1; x <= x2; x++) {
+-            if (!in_bounds(y, x)) continue;
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+-        }
+-    }
++    /* 2. Carve Maze using recursive backtracker */
++    /* Starting point: y1+1, x1+1 */
++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
+
+     /* 3. Add Monsters and Items in stamped areas */
+     for (y = y1 + 1; y < y2; y++) {
+@@ -5668,7 +5682,7 @@ static void build_sector_dark(int y0, int x0)
+         /* Open one more on the opposite side to make a passage if possible */
+         if (top_opened || left_opened) {
+             cave_feat[y2][x2-1] = FEAT_FLOOR;
+-        } else {
++        } else if (bottom_opened || right_opened) {
+             cave_feat[y1][x1+1] = FEAT_FLOOR;
+         }
+     }
+@@ -5951,21 +5965,13 @@ static void jitter_sector_boundaries(void)
+ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
+
+ 			bool is_boundary = FALSE;
+-			int by = y / BLOCK_HGT;
+-			int bx = x / BLOCK_WID;
+-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-			int sect = cave_sector[by][bx];
+-
+-			int by_minus = (y - 1) / BLOCK_HGT;
+-			int bx_minus = (x - 1) / BLOCK_WID;
+-			int by_plus = (y + 1) / BLOCK_HGT;
+-			int bx_plus = (x + 1) / BLOCK_WID;
+-
+-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
+-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
+-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
+-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
++			int sect = cave_sector[y][x];
++
++
++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
+
+ 			if (is_boundary)
+ 			{
+@@ -6040,12 +6046,9 @@ static void smooth_caverns(void)
+ 			for (x = 1; x < DUNGEON_WID - 1; x++)
+ 			{
+ 				int by = y / BLOCK_HGT;
+-				int bx = x / BLOCK_WID;
+-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+
+ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
+-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
+
+ 				/* Do not overwrite permanent features or stairs/doors */
+ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
+@@ -6209,7 +6212,7 @@ static void cave_gen(void)
+ 	/* Initialize Elevation */
+ 	init_elevation();
+
+-	/* Initialize Sector Map to RUINS by default */
++	/* Initialize Sector Map to RUINS by default for all blocks */
+ 	for (y = 0; y < dun->row_rooms; y++)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x++)
+@@ -6331,7 +6334,7 @@ static void cave_gen(void)
+ 		if (room_build(y, x, 1))
+ 		{
+ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
+-			if (cave_sector[y][x] == SECTOR_RUINS)
++			if (dun->sector_map[y][x] == SECTOR_RUINS)
+ 			{
+ 				int cy, cx;
+ 				for (cy = y - 1; cy <= y + 1; cy++)
+@@ -6376,17 +6379,26 @@ static void cave_gen(void)
+ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
+ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
+
+-			/* Assign to 2x2 block */
+-			cave_sector[y][x] = sect_type;
+-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
+-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
+-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
++			/* Assign to all blocks in the 2x2 sector group */
++			dun->sector_map[y][x] = sect_type;
++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
+ 		}
+ 	}
+
+ 	/* No "crowded" rooms yet */
+ 	dun->crowded = FALSE;
+
++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
++	for (y = 0; y < DUNGEON_HGT; y++)
++	{
++		for (x = 0; x < DUNGEON_WID; x++)
++		{
++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
++		}
++	}
++
+
+ 	/* No rooms yet */
+ 	dun->cent_n = 0;
+@@ -6396,7 +6408,7 @@ static void cave_gen(void)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x += 2)
+ 		{
+-			int sect = cave_sector[y][x];
++			int sect = dun->sector_map[y][x];
+ 			if (sect == SECTOR_SHIFTING)
+ 				{
+ 					build_sector_shifting_maze(y, x);
+@@ -6643,16 +6655,6 @@ static void cave_gen(void)
+ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
+ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
+
+-		for (sy = 0; sy < DUNGEON_HGT; sy++) {
+-			for (sx = 0; sx < DUNGEON_WID; sx++) {
+-				if (cave_feat[sy][sx] == start_feat) {
+-					set_generation_origin(sy, sx);
+-					found_start = TRUE;
+-					break;
+-				}
+-			}
+-			if (found_start) break;
+-		}
+ 		if (!found_start) {
+ 			/* Fallback to center */
+ 			set_generation_origin(DUNGEON_HGT / 2, DUNGEON_WID / 2);
+@@ -6792,6 +6794,7 @@ static void cave_gen(void)
+ 	}
+
+ 	free(dun_body);
++	dun = NULL;
+ }
+
+
diff --git a/dungeon_patch.diff b/dungeon_patch.diff
new file mode 100644
index 0000000..8990cc1
--- /dev/null
+++ b/dungeon_patch.diff
@@ -0,0 +1,59 @@
+--- src/dungeon.c
++++ src/dungeon.c
+@@ -902,48 +902,6 @@ static void process_environment(void)
+     check_player_environment();
+ }
+
+-/*
+- * Rearrange Dark Sectors
+- */
+-static void rearrange_dark_sectors(void)
+-{
+-	int y, x;
+-	/* Iterate over dungeon */
+-	for (y = 1; y < DUNGEON_HGT - 1; y++) {
+-		for (x = 1; x < DUNGEON_WID - 1; x++) {
+-			if (cave_sector[y][x] == SECTOR_DARK) {
+-				/* Skip visible grids */
+-				if (cave_info[y][x] & CAVE_VIEW) continue;
+-				/* Skip perma grids */
+-				if (cave_perma_bold(y, x)) continue;
+-				/* Skip monsters/objects */
+-				if (cave_m_idx[y][x] != 0) continue;
+-				if (cave_o_idx[y][x]) continue;
+-
+-				/* 5% chance to swap */
+-				if (rand_int(100) < 5) {
+-					if (cave_feat[y][x] == FEAT_FLOOR) {
+-						cave_set_feat(y, x, FEAT_WALL_EXTRA);
+-					} else if (cave_feat[y][x] == FEAT_WALL_EXTRA) {
+-						cave_set_feat(y, x, FEAT_FLOOR);
+-					}
+-				}
+-			}
+-		}
+-	}
+-
+-	/* Check player pinning */
+-	if (cave_sector[p_ptr->py][p_ptr->px] == SECTOR_DARK) {
+-		if (cave_feat[p_ptr->py][p_ptr->px] == FEAT_WALL_EXTRA) {
+-			msg_print("The rock around you groans and thirsts for your light.");
+-            /* Pin the player */
+-            if (!p_ptr->paralyzed) {
+-                set_paralyzed(50);
+-            }
+-		}
+-	}
+-}
+-
+ /*
+  * Get sector type of a location
+  */
+@@ -1049,7 +1007,6 @@ static void process_world(void)
+     shift_timer++;
+     if (shift_timer >= 50) {
+         shift_timer = 0;
+-        rearrange_dark_sectors();
+     }
+
+ 	/* Every 10 game turns */
diff --git a/jules_final.patch b/jules_final.patch
new file mode 100644
index 0000000..e0e58ad
--- /dev/null
+++ b/jules_final.patch
@@ -0,0 +1,426 @@
+--- src/cave.c
++++ src/cave.c
+@@ -2568,6 +2568,12 @@
+ 		over = MAX_SIGHT * 3 / 2;
+ 	}
+
++	/* Limit vision radius in SECTOR_DARK to 1 */
++	if (cave_sector[py][px] == SECTOR_DARK) {
++		full = 1;
++		over = 1;
++	}
++
+
+ 	/*** Step 0 -- Begin ***/
+
+--- src/dungeon.c
++++ src/dungeon.c
+@@ -902,48 +902,6 @@
+     check_player_environment();
+ }
+
+-/*
+- * Rearrange Dark Sectors
+- */
+-static void rearrange_dark_sectors(void)
+-{
+-	int y, x;
+-	/* Iterate over dungeon */
+-	for (y = 1; y < DUNGEON_HGT - 1; y++) {
+-		for (x = 1; x < DUNGEON_WID - 1; x++) {
+-			if (cave_sector[y][x] == SECTOR_DARK) {
+-				/* Skip visible grids */
+-				if (cave_info[y][x] & CAVE_VIEW) continue;
+-				/* Skip perma grids */
+-				if (cave_perma_bold(y, x)) continue;
+-				/* Skip monsters/objects */
+-				if (cave_m_idx[y][x] != 0) continue;
+-				if (cave_o_idx[y][x]) continue;
+-
+-				/* 5% chance to swap */
+-				if (rand_int(100) < 5) {
+-					if (cave_feat[y][x] == FEAT_FLOOR) {
+-						cave_set_feat(y, x, FEAT_WALL_EXTRA);
+-					} else if (cave_feat[y][x] == FEAT_WALL_EXTRA) {
+-						cave_set_feat(y, x, FEAT_FLOOR);
+-					}
+-				}
+-			}
+-		}
+-	}
+-
+-	/* Check player pinning */
+-	if (cave_sector[p_ptr->py][p_ptr->px] == SECTOR_DARK) {
+-		if (cave_feat[p_ptr->py][p_ptr->px] == FEAT_WALL_EXTRA) {
+-			msg_print("The rock around you groans and thirsts for your light.");
+-            /* Pin the player */
+-            if (!p_ptr->paralyzed) {
+-                set_paralyzed(50);
+-            }
+-		}
+-	}
+-}
+-
+ /*
+  * Get sector type of a location
+  */
+@@ -1049,7 +1007,6 @@
+     shift_timer++;
+     if (shift_timer >= 50) {
+         shift_timer = 0;
+-        rearrange_dark_sectors();
+     }
+
+ 	/* Every 10 game turns */
+--- src/generate.c
++++ src/generate.c
+@@ -315,6 +315,9 @@
+ 	/* Array of which blocks are used */
+ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+
++	/* Array of sector types for each block */
++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+ 	/* Hack -- there is a pit/nest on this level */
+ 	bool crowded;
+ };
+@@ -707,9 +710,9 @@
+  */
+ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+ {
+-	int y, x, j;
++	int y, x, j, sy, sx;
+
+-	/* The walls and num parameters are unused */
++	/* The walls and num parameters are mostly unused in the sector-based version */
+ 	(void)walls;
+ 	(void)num;
+
+@@ -734,7 +737,28 @@
+     }
+
++	/* Fallback for Town/Wilderness generation where 'dun' is not initialized */
++	if (!dun)
++	{
++		for (j = 0; j < 3000; j++)
++		{
++			y = rand_int(DUNGEON_HGT);
++			x = rand_int(DUNGEON_WID);
++			if (!cave_safe_bold(y, x)) continue;
++
++			if (!p_ptr->depth) {
++				if (p_ptr->inside_special == SPECIAL_WILD) cave_feat[y][x] = FEAT_SHAFT;
++				else cave_feat[y][x] = FEAT_MORE;
++			} else if (p_ptr->inside_special == SPECIAL_QUEST || (p_ptr->depth >= MAX_DEPTH - 1)) {
++				cave_feat[y][x] = FEAT_LESS;
++			} else {
++				cave_feat[y][x] = feat;
++			}
++			cave_info[y][x] |= CAVE_GLOW;
++			return;
++		}
++		return;
++	}
++
+ 	/* Allocate exactly 1 stair per 2x2 block sector */
+-	int sy, sx;
+ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
+ 	{
+ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
+@@ -953,11 +977,7 @@
+ 		/* Agent-based Drunkard's Walk */
+ 		int life = 500 + randint(1000);
+
+-		int by = y / BLOCK_HGT;
+-		int bx = x / BLOCK_WID;
+-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-		int current_sector = cave_sector[by][bx];
++		int current_sector = cave_sector[y][x];
+
+ 		int steps_between_blobs = rand_range(2, 10);
+ 		int blob_length_remaining = 0;
+@@ -1004,13 +1024,8 @@
+ 							}
+
+ 							/* Streams cannot cut through True Dark Mazes */
+-							if (valid)
++							if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
+ 							{
+-								int s_y = cy / BLOCK_HGT;
+-								int s_x = cx / BLOCK_WID;
+-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
+-								{
+-									valid = FALSE;
+-								}
++								valid = FALSE;
+ 							}
+
+@@ -1036,11 +1051,7 @@
+
+ 			if (in_bounds(ny, nx))
+ 			{
+-				int nby = ny / BLOCK_HGT;
+-				int nbx = nx / BLOCK_WID;
+-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
+-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
+-				int next_sector = cave_sector[nby][nbx];
++				int next_sector = cave_sector[ny][nx];
+
+ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
+ 				if (next_sector != current_sector)
+@@ -4395,12 +4406,15 @@
+   Rand_quick = TRUE;
+   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
+
+-  /* Clear the rest of the level. */
+-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
+-    for (x = 2; x < DUNGEON_WID - 2; x++) {
++	/* Clear the rest of the level and sector map. */
++	for (y = 0; y < DUNGEON_HGT; y++) {
++		for (x = 0; x < DUNGEON_WID; x++) {
++			cave_sector[y][x] = SECTOR_RUINS;
++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
+
+       /* Create level background */
+       cave_feat[y][x] = level_bg;
++			}
+     }
+   }
+
+@@ -5523,58 +5537,78 @@
+     }
+ }
+
++/*
++ * Recursive helper to carve the Dark Maze
++ */
++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
++{
++    int i, d;
++    int dirs[4] = { 0, 1, 2, 3 };
++
++    /* Shuffle directions */
++    for (i = 0; i < 4; i++)
++    {
++        int j = rand_int(4);
++        int temp = dirs[i];
++        dirs[i] = dirs[j];
++        dirs[j] = temp;
++    }
++
++    for (i = 0; i < 4; i++)
++    {
++        int dy = 0, dx = 0;
++        d = dirs[i];
++        if (d == 0) dy = -2;
++        else if (d == 1) dy = 2;
++        else if (d == 2) dx = -2;
++        else if (d == 3) dx = 2;
++
++        int ny = y + dy;
++        int nx = x + dx;
++
++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
++            cave_sector[ny][nx] == SECTOR_DARK)
++        {
++            cave_feat[ny][nx] = FEAT_FLOOR;
++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
++        }
++    }
++}
++
+ /*
+  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+  */
+ static void build_sector_dark(int y0, int x0)
+ {
+-    int y, x, i;
++    int y, x;
+     int y1 = y0 * BLOCK_HGT;
+     int x1 = x0 * BLOCK_WID;
+     int y2 = (y0 + 2) * BLOCK_HGT - 1;
+     int x2 = (x0 + 2) * BLOCK_WID - 1;
+
+-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
++    /* 1. Fill area with Walls and 'Darkness' flags */
+     for (y = y1; y <= y2; y++) {
+         for (x = x1; x <= x2; x++) {
+             if (!in_bounds(y, x)) continue;
+-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
++
++            /* Overwrite EVERYTHING to ensure a clean maze */
+             cave_feat[y][x] = FEAT_WALL_INNER;
+-            /* Outer boundary walls are diggable */
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+             cave_info[y][x] |= (CAVE_ROOM);
+             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
+         }
+     }
+
+-    /* 2. Carve Maze using square universal stamps of floor */
+-    stamp_type floor_stamp;
+-    floor_stamp.feat = FEAT_FLOOR;
+-    floor_stamp.elev = ELEV_GROUND;
+-    floor_stamp.radius = 2;
+-    floor_stamp.threshold = 90; /* high chance to stamp floor */
+-    floor_stamp.roughness = 20;
+-    floor_stamp.overwrite = TRUE;
+-    floor_stamp.circular = FALSE; /* Square brush */
+-
+-    int num_stamps = rand_range(15, 25);
+-    for (i = 0; i < num_stamps; i++) {
+-        int ty = rand_range(y1 + 2, y2 - 2);
+-        int tx = rand_range(x1 + 2, x2 - 2);
+-        universal_stamp(ty, tx, floor_stamp);
+-    }
+-
+-    /* Ensure outer walls remain solid except at access points */
+-    for (y = y1; y <= y2; y++) {
+-        for (x = x1; x <= x2; x++) {
+-            if (!in_bounds(y, x)) continue;
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+-        }
+-    }
++    /* 2. Carve Maze using recursive backtracker */
++    /* Starting point: y1+1, x1+1 */
++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
+
+     /* 3. Add Monsters and Items in stamped areas */
+     for (y = y1 + 1; y < y2; y++) {
+@@ -5668,7 +5702,7 @@
+         /* Open one more on the opposite side to make a passage if possible */
+         if (top_opened || left_opened) {
+             cave_feat[y2][x2-1] = FEAT_FLOOR;
+-        } else {
++        } else if (bottom_opened || right_opened) {
+             cave_feat[y1][x1+1] = FEAT_FLOOR;
+         }
+     }
+@@ -5951,21 +5985,13 @@
+ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
+
+ 			bool is_boundary = FALSE;
+-			int by = y / BLOCK_HGT;
+-			int bx = x / BLOCK_WID;
+-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-			int sect = cave_sector[by][bx];
+-
+-			int by_minus = (y - 1) / BLOCK_HGT;
+-			int bx_minus = (x - 1) / BLOCK_WID;
+-			int by_plus = (y + 1) / BLOCK_HGT;
+-			int bx_plus = (x + 1) / BLOCK_WID;
+-
+-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
+-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
+-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
+-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
++			int sect = cave_sector[y][x];
++
++
++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
+
+ 			if (is_boundary)
+ 			{
+@@ -6040,12 +6066,9 @@
+ 			for (x = 1; x < DUNGEON_WID - 1; x++)
+ 			{
+ 				int by = y / BLOCK_HGT;
+-				int bx = x / BLOCK_WID;
+-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+
+ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
+-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
+
+ 				/* Do not overwrite permanent features or stairs/doors */
+ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
+@@ -6209,7 +6232,7 @@
+ 	/* Initialize Elevation */
+ 	init_elevation();
+
+-	/* Initialize Sector Map to RUINS by default */
++	/* Initialize Sector Map to RUINS by default for all blocks */
+ 	for (y = 0; y < dun->row_rooms; y++)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x++)
+@@ -6331,7 +6354,7 @@
+ 		if (room_build(y, x, 1))
+ 		{
+ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
+-			if (cave_sector[y][x] == SECTOR_RUINS)
++			if (dun->sector_map[y][x] == SECTOR_RUINS)
+ 			{
+ 				int cy, cx;
+ 				for (cy = y - 1; cy <= y + 1; cy++)
+@@ -6376,17 +6399,26 @@
+ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
+ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
+
+-			/* Assign to 2x2 block */
+-			cave_sector[y][x] = sect_type;
+-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
+-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
+-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
++			/* Assign to all blocks in the 2x2 sector group */
++			dun->sector_map[y][x] = sect_type;
++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
+ 		}
+ 	}
+
+ 	/* No "crowded" rooms yet */
+ 	dun->crowded = FALSE;
+
++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
++	for (y = 0; y < DUNGEON_HGT; y++)
++	{
++		for (x = 0; x < DUNGEON_WID; x++)
++		{
++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
++		}
++	}
++
+
+ 	/* No rooms yet */
+ 	dun->cent_n = 0;
+@@ -6396,7 +6428,7 @@
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x += 2)
+ 		{
+-			int sect = cave_sector[y][x];
++			int sect = dun->sector_map[y][x];
+ 			if (sect == SECTOR_SHIFTING)
+ 				{
+ 					build_sector_shifting_maze(y, x);
+@@ -6643,6 +6675,7 @@
+ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
+ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
+
++		int sy, sx;
+ 		for (sy = 0; sy < DUNGEON_HGT; sy++) {
+ 			for (sx = 0; sx < DUNGEON_WID; sx++) {
+ 				if (cave_feat[sy][sx] == start_feat) {
+@@ -6792,6 +6825,7 @@
+ 	}
+
+ 	free(dun_body);
++	dun = NULL;
+ }
+
+
diff --git a/jules_final2.patch b/jules_final2.patch
new file mode 100644
index 0000000..4885985
--- /dev/null
+++ b/jules_final2.patch
@@ -0,0 +1,352 @@
+--- src/generate.c
++++ src/generate.c
+@@ -315,6 +315,9 @@
+ 	/* Array of which blocks are used */
+ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+
++	/* Array of sector types for each block */
++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+ 	/* Hack -- there is a pit/nest on this level */
+ 	bool crowded;
+ };
+@@ -707,9 +710,9 @@
+  */
+ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+ {
+-	int y, x, j;
++	int y, x, j, sy, sx;
+
+-	/* The walls and num parameters are unused */
++	/* The walls and num parameters are mostly unused in the sector-based version */
+ 	(void)walls;
+ 	(void)num;
+
+@@ -734,7 +737,28 @@
+     }
+
++	/* Fallback for Town/Wilderness generation where 'dun' is not initialized */
++	if (!dun)
++	{
++		for (j = 0; j < 3000; j++)
++		{
++			y = rand_int(DUNGEON_HGT);
++			x = rand_int(DUNGEON_WID);
++			if (!cave_safe_bold(y, x)) continue;
++
++			if (!p_ptr->depth) {
++				if (p_ptr->inside_special == SPECIAL_WILD) cave_feat[y][x] = FEAT_SHAFT;
++				else cave_feat[y][x] = FEAT_MORE;
++			} else if (p_ptr->inside_special == SPECIAL_QUEST || (p_ptr->depth >= MAX_DEPTH - 1)) {
++				cave_feat[y][x] = FEAT_LESS;
++			} else {
++				cave_feat[y][x] = feat;
++			}
++			cave_info[y][x] |= CAVE_GLOW;
++			return;
++		}
++		return;
++	}
++
+ 	/* Allocate exactly 1 stair per 2x2 block sector */
+-	int sy, sx;
+ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
+ 	{
+ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
+@@ -953,11 +977,7 @@
+ 		/* Agent-based Drunkard's Walk */
+ 		int life = 500 + randint(1000);
+
+-		int by = y / BLOCK_HGT;
+-		int bx = x / BLOCK_WID;
+-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-		int current_sector = cave_sector[by][bx];
++		int current_sector = cave_sector[y][x];
+
+ 		int steps_between_blobs = rand_range(2, 10);
+ 		int blob_length_remaining = 0;
+@@ -1004,13 +1024,8 @@
+ 							}
+
+ 							/* Streams cannot cut through True Dark Mazes */
+-							if (valid)
++							if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
+ 							{
+-								int s_y = cy / BLOCK_HGT;
+-								int s_x = cx / BLOCK_WID;
+-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
+-								{
+-									valid = FALSE;
+-								}
++								valid = FALSE;
+ 							}
+
+@@ -1036,11 +1051,7 @@
+
+ 			if (in_bounds(ny, nx))
+ 			{
+-				int nby = ny / BLOCK_HGT;
+-				int nbx = nx / BLOCK_WID;
+-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
+-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
+-				int next_sector = cave_sector[nby][nbx];
++				int next_sector = cave_sector[ny][nx];
+
+ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
+ 				if (next_sector != current_sector)
+@@ -4395,12 +4406,15 @@
+   Rand_quick = TRUE;
+   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
+
+-  /* Clear the rest of the level. */
+-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
+-    for (x = 2; x < DUNGEON_WID - 2; x++) {
++	/* Clear the rest of the level and sector map. */
++	for (y = 0; y < DUNGEON_HGT; y++) {
++		for (x = 0; x < DUNGEON_WID; x++) {
++			cave_sector[y][x] = SECTOR_RUINS;
++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
+
+       /* Create level background */
+       cave_feat[y][x] = level_bg;
++			}
+     }
+   }
+
+@@ -5523,58 +5537,78 @@
+     }
+ }
+
++/*
++ * Recursive helper to carve the Dark Maze
++ */
++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
++{
++    int i, d;
++    int dirs[4] = { 0, 1, 2, 3 };
++
++    /* Shuffle directions */
++    for (i = 0; i < 4; i++)
++    {
++        int j = rand_int(4);
++        int temp = dirs[i];
++        dirs[i] = dirs[j];
++        dirs[j] = temp;
++    }
++
++    for (i = 0; i < 4; i++)
++    {
++        int dy = 0, dx = 0;
++        d = dirs[i];
++        if (d == 0) dy = -2;
++        else if (d == 1) dy = 2;
++        else if (d == 2) dx = -2;
++        else if (d == 3) dx = 2;
++
++        int ny = y + dy;
++        int nx = x + dx;
++
++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
++            cave_sector[ny][nx] == SECTOR_DARK)
++        {
++            cave_feat[ny][nx] = FEAT_FLOOR;
++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
++        }
++    }
++}
++
+ /*
+  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+  */
+ static void build_sector_dark(int y0, int x0)
+ {
+-    int y, x, i;
++    int y, x;
+     int y1 = y0 * BLOCK_HGT;
+     int x1 = x0 * BLOCK_WID;
+     int y2 = (y0 + 2) * BLOCK_HGT - 1;
+     int x2 = (x0 + 2) * BLOCK_WID - 1;
+
+-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
++    /* 1. Fill area with Walls and 'Darkness' flags */
+     for (y = y1; y <= y2; y++) {
+         for (x = x1; x <= x2; x++) {
+             if (!in_bounds(y, x)) continue;
+-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
++
++            /* Overwrite EVERYTHING to ensure a clean maze */
+             cave_feat[y][x] = FEAT_WALL_INNER;
+-            /* Outer boundary walls are diggable */
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+             cave_info[y][x] |= (CAVE_ROOM);
+             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
+         }
+     }
+
+-    /* 2. Carve Maze using square universal stamps of floor */
+-    stamp_type floor_stamp;
+-    floor_stamp.feat = FEAT_FLOOR;
+-    floor_stamp.elev = ELEV_GROUND;
+-    floor_stamp.radius = 2;
+-    floor_stamp.threshold = 90; /* high chance to stamp floor */
+-    floor_stamp.roughness = 20;
+-    floor_stamp.overwrite = TRUE;
+-    floor_stamp.circular = FALSE; /* Square brush */
+-
+-    int num_stamps = rand_range(15, 25);
+-    for (i = 0; i < num_stamps; i++) {
+-        int ty = rand_range(y1 + 2, y2 - 2);
+-        int tx = rand_range(x1 + 2, x2 - 2);
+-        universal_stamp(ty, tx, floor_stamp);
+-    }
+-
+-    /* Ensure outer walls remain solid except at access points */
+-    for (y = y1; y <= y2; y++) {
+-        for (x = x1; x <= x2; x++) {
+-            if (!in_bounds(y, x)) continue;
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+-        }
+-    }
++    /* 2. Carve Maze using recursive backtracker */
++    /* Starting point: y1+1, x1+1 */
++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
+
+     /* 3. Add Monsters and Items in stamped areas */
+     for (y = y1 + 1; y < y2; y++) {
+@@ -5668,7 +5702,7 @@
+         /* Open one more on the opposite side to make a passage if possible */
+         if (top_opened || left_opened) {
+             cave_feat[y2][x2-1] = FEAT_FLOOR;
+-        } else {
++        } else if (bottom_opened || right_opened) {
+             cave_feat[y1][x1+1] = FEAT_FLOOR;
+         }
+     }
+@@ -5951,21 +5985,13 @@
+ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
+
+ 			bool is_boundary = FALSE;
+-			int by = y / BLOCK_HGT;
+-			int bx = x / BLOCK_WID;
+-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-			int sect = cave_sector[by][bx];
+-
+-			int by_minus = (y - 1) / BLOCK_HGT;
+-			int bx_minus = (x - 1) / BLOCK_WID;
+-			int by_plus = (y + 1) / BLOCK_HGT;
+-			int bx_plus = (x + 1) / BLOCK_WID;
+-
+-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
+-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
+-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
+-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
++			int sect = cave_sector[y][x];
++
++
++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
+
+ 			if (is_boundary)
+ 			{
+@@ -6040,12 +6066,9 @@
+ 			for (x = 1; x < DUNGEON_WID - 1; x++)
+ 			{
+ 				int by = y / BLOCK_HGT;
+-				int bx = x / BLOCK_WID;
+-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+
+ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
+-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
+
+ 				/* Do not overwrite permanent features or stairs/doors */
+ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
+@@ -6209,7 +6232,7 @@
+ 	/* Initialize Elevation */
+ 	init_elevation();
+
+-	/* Initialize Sector Map to RUINS by default */
++	/* Initialize Sector Map to RUINS by default for all blocks */
+ 	for (y = 0; y < dun->row_rooms; y++)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x++)
+@@ -6331,7 +6354,7 @@
+ 		if (room_build(y, x, 1))
+ 		{
+ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
+-			if (cave_sector[y][x] == SECTOR_RUINS)
++			if (dun->sector_map[y][x] == SECTOR_RUINS)
+ 			{
+ 				int cy, cx;
+ 				for (cy = y - 1; cy <= y + 1; cy++)
+@@ -6376,17 +6399,26 @@
+ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
+ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
+
+-			/* Assign to 2x2 block */
+-			cave_sector[y][x] = sect_type;
+-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
+-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
+-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
++			/* Assign to all blocks in the 2x2 sector group */
++			dun->sector_map[y][x] = sect_type;
++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
+ 		}
+ 	}
+
+ 	/* No "crowded" rooms yet */
+ 	dun->crowded = FALSE;
+
++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
++	for (y = 0; y < DUNGEON_HGT; y++)
++	{
++		for (x = 0; x < DUNGEON_WID; x++)
++		{
++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
++		}
++	}
++
+
+ 	/* No rooms yet */
+ 	dun->cent_n = 0;
+@@ -6396,7 +6428,7 @@
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x += 2)
+ 		{
+-			int sect = cave_sector[y][x];
++			int sect = dun->sector_map[y][x];
+ 			if (sect == SECTOR_SHIFTING)
+ 				{
+ 					build_sector_shifting_maze(y, x);
+@@ -6643,6 +6675,7 @@
+ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
+ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
+
++		int sy, sx;
+ 		for (sy = 0; sy < DUNGEON_HGT; sy++) {
+ 			for (sx = 0; sx < DUNGEON_WID; sx++) {
+ 				if (cave_feat[sy][sx] == start_feat) {
+@@ -6792,6 +6825,7 @@
+ 	}
+
+ 	free(dun_body);
++	dun = NULL;
+ }
+
+
diff --git a/kamband_stats.csv b/kamband_stats.csv
new file mode 100644
index 0000000..dda3f10
--- /dev/null
+++ b/kamband_stats.csv
@@ -0,0 +1,2 @@
+turn,event_type,duration_ms,mem_kb,depth,x,y,elevation
+1,level_gen,6911,10496,29,365,58,0
diff --git a/lib/data/a_info.raw b/lib/data/a_info.raw
new file mode 100644
index 0000000..97245a0
Binary files /dev/null and b/lib/data/a_info.raw differ
diff --git a/lib/data/e_info.raw b/lib/data/e_info.raw
new file mode 100644
index 0000000..1323fe7
Binary files /dev/null and b/lib/data/e_info.raw differ
diff --git a/lib/data/f_info.raw b/lib/data/f_info.raw
new file mode 100644
index 0000000..6bc9a4f
Binary files /dev/null and b/lib/data/f_info.raw differ
diff --git a/lib/data/k_info.raw b/lib/data/k_info.raw
new file mode 100644
index 0000000..f4bc9c7
Binary files /dev/null and b/lib/data/k_info.raw differ
diff --git a/lib/data/r_info.raw b/lib/data/r_info.raw
new file mode 100644
index 0000000..0b7ee1b
Binary files /dev/null and b/lib/data/r_info.raw differ
diff --git a/lib/data/v_info.raw b/lib/data/v_info.raw
new file mode 100644
index 0000000..f3abcc6
Binary files /dev/null and b/lib/data/v_info.raw differ
diff --git a/lib/save/1001.Jules b/lib/save/1001.Jules
new file mode 100644
index 0000000..626d5ca
Binary files /dev/null and b/lib/save/1001.Jules differ
diff --git a/load_debug.log b/load_debug.log
new file mode 100644
index 0000000..e806fd4
--- /dev/null
+++ b/load_debug.log
@@ -0,0 +1,26 @@
+rd_savefile_new: start
+Loading a 2.1.9 savefile...
+rd_extra: start
+rd_extra: seeds read
+rd_extra: death flag read
+Loaded Turn: 1
+Starting to restore Dungeon...
+Restoring Dungeon...
+Dungeon Header: Depth 29, PY 58, PX 365, YMAX 440, XMAX 462
+rd_dungeon: start RLE 1 (info)
+rd_dungeon: start RLE 2 (fire life)
+rd_dungeon: start RLE 3 (feat)
+rd_dungeon: start RLE 4 (sector)
+rd_savefile_new: start
+Loading a 2.1.9 savefile...
+rd_extra: start
+rd_extra: seeds read
+rd_extra: death flag read
+Loaded Turn: 1
+Starting to restore Dungeon...
+Restoring Dungeon...
+Dungeon Header: Depth 29, PY 58, PX 365, YMAX 440, XMAX 462
+rd_dungeon: start RLE 1 (info)
+rd_dungeon: start RLE 2 (fire life)
+rd_dungeon: start RLE 3 (feat)
+rd_dungeon: start RLE 4 (sector)
diff --git a/patch.diff b/patch.diff
index a04d6b9..e69de29 100644
--- a/patch.diff
+++ b/patch.diff
@@ -1,61 +0,0 @@
-<<<<<<< SEARCH
-/*
- * Swap the players/monsters (if any) at two locations XXX XXX XXX
- */
-void monster_swap(int y1, int x1, int y2, int x2)
-=======
-/* Process terrain effects for a monster at (y, x) */
-void mon_process_terrain(int m_idx, int y, int x)
-{
-	monster_type *m_ptr = &m_list[m_idx];
-	monster_race *r_ptr = &r_info[m_ptr->r_idx];
-	int feat = cave_feat[y][x];
-
-	/* Flying monsters ignore ground hazards */
-	if (r_ptr->flags2 & RF2_FLY) return;
-
-	/* Handle Oil */
-	if (feat == FEAT_OIL)
-	{
-		/* Energy penalty: slower movement */
-		m_ptr->energy -= 50;
-
-		/* Slip check: 50% chance (increased for insects) */
-		int chance = 50;
-		if (strchr("aAsS", r_ptr->d_char)) chance = 80; /* Ants/Spiders hate oil */
-
-		if (rand_int(100) < chance)
-		{
-			if (m_ptr->ml && m_ptr->is_pet)
-			{
-				char m_name[80];
-				monster_desc(m_name, m_ptr, 0);
-				msg_format("The %s slips on the oil!", m_name);
-			}
-			/* 'Knock out' - heavy stun or temporary sleep */
-			m_ptr->stunned += 10 + rand_int(20);
-			if (m_ptr->stunned > 50) m_ptr->csleep = 5; /* Actually knocked out */
-		}
-	}
-
-	/* Handle Ice */
-	if (feat == FEAT_ICE)
-	{
-		if (rand_int(100) < 30 && !(r_ptr->flags3 & RF3_IM_COLD))
-		{
-			if (m_ptr->ml && m_ptr->is_pet)
-			{
-				char m_name[80];
-				monster_desc(m_name, m_ptr, 0);
-				msg_format("The %s loses its footing on the ice!", m_name);
-			}
-			m_ptr->stunned += rand_int(10);
-		}
-	}
-}
-
-/*
- * Swap the players/monsters (if any) at two locations XXX XXX XXX
- */
-void monster_swap(int y1, int x1, int y2, int x2)
->>>>>>> REPLACE
diff --git a/src/generate.c b/src/generate.c
index 17ac01d..4d3d862 100644
--- a/src/generate.c
+++ b/src/generate.c
@@ -315,6 +315,9 @@ struct dun_data
	/* Array of which blocks are used */
	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];

+	/* Array of sector types for each block */
+	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+
	/* Hack -- there is a pit/nest on this level */
	bool crowded;
 };
@@ -707,9 +710,9 @@ static void place_random_door(int y, int x)
  */
 static void alloc_stairs(int feat, int num, int walls, bool force_room)
 {
-	int y, x, j;
+	int y, x, j, sy, sx;

-	/* The walls and num parameters are unused */
+	/* The walls and num parameters are mostly unused in the sector-based version */
	(void)walls;
	(void)num;

@@ -734,7 +737,6 @@ static void alloc_stairs(int feat, int num, int walls, bool force_room)
     }

	/* Allocate exactly 1 stair per 2x2 block sector */
-	int sy, sx;
	for (sy = 0; sy < dun->row_rooms; sy += 2)
	{
		for (sx = 0; sx < dun->col_rooms; sx += 2)
@@ -953,11 +955,7 @@ static void build_streamer2(int feat, int killwall)
		/* Agent-based Drunkard's Walk */
		int life = 500 + randint(1000);

-		int by = y / BLOCK_HGT;
-		int bx = x / BLOCK_WID;
-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
-		int current_sector = cave_sector[by][bx];
+		int current_sector = cave_sector[y][x];

		int steps_between_blobs = rand_range(2, 10);
		int blob_length_remaining = 0;
@@ -1004,13 +1002,10 @@ static void build_streamer2(int feat, int killwall)
							}

							/* Streams cannot cut through True Dark Mazes */
-							if (valid)
+						if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
							{
-								int s_y = cy / BLOCK_HGT;
-								int s_x = cx / BLOCK_WID;
-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
+							valid = FALSE;
								{
-									valid = FALSE;
								}
							}

@@ -1036,11 +1031,7 @@ static void build_streamer2(int feat, int killwall)

			if (in_bounds(ny, nx))
			{
-				int nby = ny / BLOCK_HGT;
-				int nbx = nx / BLOCK_WID;
-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
-				int next_sector = cave_sector[nby][nbx];
+				int next_sector = cave_sector[ny][nx];

				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
				if (next_sector != current_sector)
@@ -4395,12 +4386,15 @@ static void terrain_gen(void) {
   Rand_quick = TRUE;
   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);

-  /* Clear the rest of the level. */
-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
-    for (x = 2; x < DUNGEON_WID - 2; x++) {
+	/* Clear the rest of the level and sector map. */
+	for (y = 0; y < DUNGEON_HGT; y++) {
+		for (x = 0; x < DUNGEON_WID; x++) {
+			cave_sector[y][x] = SECTOR_RUINS;
+			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {

       /* Create level background */
       cave_feat[y][x] = level_bg;
+			}
     }
   }

@@ -5523,58 +5517,78 @@ static void build_sector_shifting_maze(int y0, int x0)
     }
 }

+/*
+ * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+ */
+/*
+ * Recursive helper to carve the Dark Maze
+ */
+static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
+{
+    int i, d;
+    int dirs[4] = { 0, 1, 2, 3 };
+
+    /* Shuffle directions */
+    for (i = 0; i < 4; i++)
+    {
+        int j = rand_int(4);
+        int temp = dirs[i];
+        dirs[i] = dirs[j];
+        dirs[j] = temp;
+    }
+
+    for (i = 0; i < 4; i++)
+    {
+        int dy = 0, dx = 0;
+        d = dirs[i];
+        if (d == 0) dy = -2;
+        else if (d == 1) dy = 2;
+        else if (d == 2) dx = -2;
+        else if (d == 3) dx = 2;
+
+        int ny = y + dy;
+        int nx = x + dx;
+
+        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
+        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
+            cave_feat[ny][nx] == FEAT_WALL_INNER &&
+            cave_sector[ny][nx] == SECTOR_DARK)
+        {
+            cave_feat[ny][nx] = FEAT_FLOOR;
+            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
+            carve_maze_dark(ny, nx, y1, x1, y2, x2);
+        }
+    }
+}
+
 /*
  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
  */
 static void build_sector_dark(int y0, int x0)
 {
-    int y, x, i;
+    int y, x;
     int y1 = y0 * BLOCK_HGT;
     int x1 = x0 * BLOCK_WID;
     int y2 = (y0 + 2) * BLOCK_HGT - 1;
     int x2 = (x0 + 2) * BLOCK_WID - 1;

-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
+    /* 1. Fill area with Walls and 'Darkness' flags */
     for (y = y1; y <= y2; y++) {
         for (x = x1; x <= x2; x++) {
             if (!in_bounds(y, x)) continue;
-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
+
+            /* Overwrite EVERYTHING to ensure a clean maze */
             cave_feat[y][x] = FEAT_WALL_INNER;
-            /* Outer boundary walls are diggable */
-            if (y == y1 || y == y2 || x == x1 || x == x2) {
-                cave_feat[y][x] = FEAT_MAGMA;
-            }
             cave_info[y][x] |= (CAVE_ROOM);
             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
+            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
         }
     }

-    /* 2. Carve Maze using square universal stamps of floor */
-    stamp_type floor_stamp;
-    floor_stamp.feat = FEAT_FLOOR;
-    floor_stamp.elev = ELEV_GROUND;
-    floor_stamp.radius = 2;
-    floor_stamp.threshold = 90; /* high chance to stamp floor */
-    floor_stamp.roughness = 20;
-    floor_stamp.overwrite = TRUE;
-    floor_stamp.circular = FALSE; /* Square brush */
-
-    int num_stamps = rand_range(15, 25);
-    for (i = 0; i < num_stamps; i++) {
-        int ty = rand_range(y1 + 2, y2 - 2);
-        int tx = rand_range(x1 + 2, x2 - 2);
-        universal_stamp(ty, tx, floor_stamp);
-    }
-
-    /* Ensure outer walls remain solid except at access points */
-    for (y = y1; y <= y2; y++) {
-        for (x = x1; x <= x2; x++) {
-            if (!in_bounds(y, x)) continue;
-            if (y == y1 || y == y2 || x == x1 || x == x2) {
-                cave_feat[y][x] = FEAT_MAGMA;
-            }
-        }
-    }
+    /* 2. Carve Maze using recursive backtracker */
+    /* Starting point: y1+1, x1+1 */
+    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
+    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);

     /* 3. Add Monsters and Items in stamped areas */
     for (y = y1 + 1; y < y2; y++) {
@@ -5668,7 +5682,7 @@ static void build_sector_dark(int y0, int x0)
         /* Open one more on the opposite side to make a passage if possible */
         if (top_opened || left_opened) {
             cave_feat[y2][x2-1] = FEAT_FLOOR;
-        } else {
+        } else if (bottom_opened || right_opened) {
             cave_feat[y1][x1+1] = FEAT_FLOOR;
         }
     }
@@ -5951,21 +5965,13 @@ static void jitter_sector_boundaries(void)
			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;

			bool is_boundary = FALSE;
-			int by = y / BLOCK_HGT;
-			int bx = x / BLOCK_WID;
-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
-			int sect = cave_sector[by][bx];
-
-			int by_minus = (y - 1) / BLOCK_HGT;
-			int bx_minus = (x - 1) / BLOCK_WID;
-			int by_plus = (y + 1) / BLOCK_HGT;
-			int bx_plus = (x + 1) / BLOCK_WID;
-
-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
+			int sect = cave_sector[y][x];
+
+
+			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
+			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
+			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
+			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;

			if (is_boundary)
			{
@@ -6040,12 +6046,9 @@ static void smooth_caverns(void)
			for (x = 1; x < DUNGEON_WID - 1; x++)
			{
				int by = y / BLOCK_HGT;
-				int bx = x / BLOCK_WID;
-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;

				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
+				if (cave_sector[y][x] != SECTOR_CAVERN) continue;

				/* Do not overwrite permanent features or stairs/doors */
				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
@@ -6209,7 +6212,7 @@ static void cave_gen(void)
	/* Initialize Elevation */
	init_elevation();

-	/* Initialize Sector Map to RUINS by default */
+	/* Initialize Sector Map to RUINS by default for all blocks */
	for (y = 0; y < dun->row_rooms; y++)
	{
		for (x = 0; x < dun->col_rooms; x++)
@@ -6331,7 +6334,7 @@ static void cave_gen(void)
		if (room_build(y, x, 1))
		{
			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
-			if (cave_sector[y][x] == SECTOR_RUINS)
+			if (dun->sector_map[y][x] == SECTOR_RUINS)
			{
				int cy, cx;
				for (cy = y - 1; cy <= y + 1; cy++)
@@ -6376,17 +6379,26 @@ static void cave_gen(void)
				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */

-			/* Assign to 2x2 block */
-			cave_sector[y][x] = sect_type;
-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
+			/* Assign to all blocks in the 2x2 sector group */
+			dun->sector_map[y][x] = sect_type;
+			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
+			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
+			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
		}
	}

	/* No "crowded" rooms yet */
	dun->crowded = FALSE;

+	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
+	for (y = 0; y < DUNGEON_HGT; y++)
+	{
+		for (x = 0; x < DUNGEON_WID; x++)
+		{
+			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
+		}
+	}
+

	/* No rooms yet */
	dun->cent_n = 0;
@@ -6396,7 +6408,7 @@ static void cave_gen(void)
	{
		for (x = 0; x < dun->col_rooms; x += 2)
		{
-			int sect = cave_sector[y][x];
+			int sect = dun->sector_map[y][x];
			if (sect == SECTOR_SHIFTING)
				{
					build_sector_shifting_maze(y, x);
@@ -6792,6 +6804,7 @@ static void cave_gen(void)
	}

	free(dun_body);
+	dun = NULL;
 }


diff --git a/src/generate.c.rej b/src/generate.c.rej
new file mode 100644
index 0000000..4e93a14
--- /dev/null
+++ b/src/generate.c.rej
@@ -0,0 +1,342 @@
+--- src/generate.c
++++ src/generate.c
+@@ -315,6 +315,9 @@ struct dun_data
+ 	/* Array of which blocks are used */
+ 	bool room_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
+
++	/* Array of sector types for each block */
++	byte sector_map[MAX_ROOMS_ROW][MAX_ROOMS_COL];
++
+ 	/* Hack -- there is a pit/nest on this level */
+ 	bool crowded;
+ };
+@@ -707,9 +710,9 @@ static void place_random_door(int y, int x)
+  */
+ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+ {
+-	int y, x, j;
++	int y, x, j, sy, sx;
+
+-	/* The walls and num parameters are unused */
++	/* The walls and num parameters are mostly unused in the sector-based version */
+ 	(void)walls;
+ 	(void)num;
+
+@@ -734,7 +737,6 @@ static void alloc_stairs(int feat, int num, int walls, bool force_room)
+     }
+
+ 	/* Allocate exactly 1 stair per 2x2 block sector */
+-	int sy, sx;
+ 	for (sy = 0; sy < dun->row_rooms; sy += 2)
+ 	{
+ 		for (sx = 0; sx < dun->col_rooms; sx += 2)
+@@ -953,11 +955,7 @@ static void build_streamer2(int feat, int killwall)
+ 		/* Agent-based Drunkard's Walk */
+ 		int life = 500 + randint(1000);
+
+-		int by = y / BLOCK_HGT;
+-		int bx = x / BLOCK_WID;
+-		if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-		if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-		int current_sector = cave_sector[by][bx];
++		int current_sector = cave_sector[y][x];
+
+ 		int steps_between_blobs = rand_range(2, 10);
+ 		int blob_length_remaining = 0;
+@@ -1004,13 +1002,10 @@ static void build_streamer2(int feat, int killwall)
+ 							}
+
+ 							/* Streams cannot cut through True Dark Mazes */
+-							if (valid)
++						if (valid && in_bounds(cy, cx) && cave_sector[cy][cx] == SECTOR_DARK)
+ 							{
+-								int s_y = cy / BLOCK_HGT;
+-								int s_x = cx / BLOCK_WID;
+-								if (s_y >= 0 && s_y < MAX_ROOMS_ROW && s_x >= 0 && s_x < MAX_ROOMS_COL && cave_sector[s_y][s_x] == SECTOR_DARK)
++							valid = FALSE;
+ 								{
+-									valid = FALSE;
+ 								}
+ 							}
+
+@@ -1036,11 +1031,7 @@ static void build_streamer2(int feat, int killwall)
+
+ 			if (in_bounds(ny, nx))
+ 			{
+-				int nby = ny / BLOCK_HGT;
+-				int nbx = nx / BLOCK_WID;
+-				if (nby >= MAX_ROOMS_ROW) nby = MAX_ROOMS_ROW - 1;
+-				if (nbx >= MAX_ROOMS_COL) nbx = MAX_ROOMS_COL - 1;
+-				int next_sector = cave_sector[nby][nbx];
++				int next_sector = cave_sector[ny][nx];
+
+ 				/* Sector overflow check (40% chance to overflow, 60% to stay within sector) */
+ 				if (next_sector != current_sector)
+@@ -4395,12 +4386,15 @@ static void terrain_gen(void) {
+   Rand_quick = TRUE;
+   Rand_value = HASH_LEVEL(p_ptr->wild_x, p_ptr->wild_y);
+
+-  /* Clear the rest of the level. */
+-  for (y = 2; y < DUNGEON_HGT - 2; y++) {
+-    for (x = 2; x < DUNGEON_WID - 2; x++) {
++	/* Clear the rest of the level and sector map. */
++	for (y = 0; y < DUNGEON_HGT; y++) {
++		for (x = 0; x < DUNGEON_WID; x++) {
++			cave_sector[y][x] = SECTOR_RUINS;
++			if (y >= 2 && y < DUNGEON_HGT - 2 && x >= 2 && x < DUNGEON_WID - 2) {
+
+       /* Create level background */
+       cave_feat[y][x] = level_bg;
++			}
+     }
+   }
+
+@@ -5523,58 +5517,78 @@ static void build_sector_shifting_maze(int y0, int x0)
+     }
+ }
+
++/*
++ * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
++ */
++/*
++ * Recursive helper to carve the Dark Maze
++ */
++static void carve_maze_dark(int y, int x, int y1, int x1, int y2, int x2)
++{
++    int i, d;
++    int dirs[4] = { 0, 1, 2, 3 };
++
++    /* Shuffle directions */
++    for (i = 0; i < 4; i++)
++    {
++        int j = rand_int(4);
++        int temp = dirs[i];
++        dirs[i] = dirs[j];
++        dirs[j] = temp;
++    }
++
++    for (i = 0; i < 4; i++)
++    {
++        int dy = 0, dx = 0;
++        d = dirs[i];
++        if (d == 0) dy = -2;
++        else if (d == 1) dy = 2;
++        else if (d == 2) dx = -2;
++        else if (d == 3) dx = 2;
++
++        int ny = y + dy;
++        int nx = x + dx;
++
++        /* Check bounds and ensure we only carve into un-carved Dark Maze walls */
++        if (ny > y1 && ny < y2 && nx > x1 && nx < x2 &&
++            cave_feat[ny][nx] == FEAT_WALL_INNER &&
++            cave_sector[ny][nx] == SECTOR_DARK)
++        {
++            cave_feat[ny][nx] = FEAT_FLOOR;
++            cave_feat[y + dy / 2][x + dx / 2] = FEAT_FLOOR;
++            carve_maze_dark(ny, nx, y1, x1, y2, x2);
++        }
++    }
++}
++
+ /*
+  * Build a Dark Maze Sector: A perfect geometric maze that restricts vision.
+  */
+ static void build_sector_dark(int y0, int x0)
+ {
+-    int y, x, i;
++    int y, x;
+     int y1 = y0 * BLOCK_HGT;
+     int x1 = x0 * BLOCK_WID;
+     int y2 = (y0 + 2) * BLOCK_HGT - 1;
+     int x2 = (x0 + 2) * BLOCK_WID - 1;
+
+-    /* 1. Fill area with Diggable Walls and 'Darkness' flags */
++    /* 1. Fill area with Walls and 'Darkness' flags */
+     for (y = y1; y <= y2; y++) {
+         for (x = x1; x <= x2; x++) {
+             if (!in_bounds(y, x)) continue;
+-            if (cave_info[y][x] & CAVE_ROOM) continue; /* Protect existing rooms */
++
++            /* Overwrite EVERYTHING to ensure a clean maze */
+             cave_feat[y][x] = FEAT_WALL_INNER;
+-            /* Outer boundary walls are diggable */
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+             cave_info[y][x] |= (CAVE_ROOM);
+             cave_info[y][x] &= ~(CAVE_GLOW | CAVE_MARK); /* Dark and Unmapped */
++            cave_sector[y][x] = SECTOR_DARK; /* Ensure tile-mapped sector */
+         }
+     }
+
+-    /* 2. Carve Maze using square universal stamps of floor */
+-    stamp_type floor_stamp;
+-    floor_stamp.feat = FEAT_FLOOR;
+-    floor_stamp.elev = ELEV_GROUND;
+-    floor_stamp.radius = 2;
+-    floor_stamp.threshold = 90; /* high chance to stamp floor */
+-    floor_stamp.roughness = 20;
+-    floor_stamp.overwrite = TRUE;
+-    floor_stamp.circular = FALSE; /* Square brush */
+-
+-    int num_stamps = rand_range(15, 25);
+-    for (i = 0; i < num_stamps; i++) {
+-        int ty = rand_range(y1 + 2, y2 - 2);
+-        int tx = rand_range(x1 + 2, x2 - 2);
+-        universal_stamp(ty, tx, floor_stamp);
+-    }
+-
+-    /* Ensure outer walls remain solid except at access points */
+-    for (y = y1; y <= y2; y++) {
+-        for (x = x1; x <= x2; x++) {
+-            if (!in_bounds(y, x)) continue;
+-            if (y == y1 || y == y2 || x == x1 || x == x2) {
+-                cave_feat[y][x] = FEAT_MAGMA;
+-            }
+-        }
+-    }
++    /* 2. Carve Maze using recursive backtracker */
++    /* Starting point: y1+1, x1+1 */
++    cave_feat[y1+1][x1+1] = FEAT_FLOOR;
++    carve_maze_dark(y1+1, x1+1, y1, x1, y2, x2);
+
+     /* 3. Add Monsters and Items in stamped areas */
+     for (y = y1 + 1; y < y2; y++) {
+@@ -5668,7 +5682,7 @@ static void build_sector_dark(int y0, int x0)
+         /* Open one more on the opposite side to make a passage if possible */
+         if (top_opened || left_opened) {
+             cave_feat[y2][x2-1] = FEAT_FLOOR;
+-        } else {
++        } else if (bottom_opened || right_opened) {
+             cave_feat[y1][x1+1] = FEAT_FLOOR;
+         }
+     }
+@@ -5951,21 +5965,13 @@ static void jitter_sector_boundaries(void)
+ 			if (cave_feat[y][x] > FEAT_WALL_SOLID) continue;
+
+ 			bool is_boundary = FALSE;
+-			int by = y / BLOCK_HGT;
+-			int bx = x / BLOCK_WID;
+-			if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-			if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+-			int sect = cave_sector[by][bx];
+-
+-			int by_minus = (y - 1) / BLOCK_HGT;
+-			int bx_minus = (x - 1) / BLOCK_WID;
+-			int by_plus = (y + 1) / BLOCK_HGT;
+-			int bx_plus = (x + 1) / BLOCK_WID;
+-
+-			if (by_minus >= 0 && cave_sector[by_minus][bx] != sect) is_boundary = TRUE;
+-			if (by_plus < MAX_ROOMS_ROW && cave_sector[by_plus][bx] != sect) is_boundary = TRUE;
+-			if (bx_minus >= 0 && cave_sector[by][bx_minus] != sect) is_boundary = TRUE;
+-			if (bx_plus < MAX_ROOMS_COL && cave_sector[by][bx_plus] != sect) is_boundary = TRUE;
++			int sect = cave_sector[y][x];
++
++
++			if (cave_sector[y - 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y + 1][x] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x - 1] != sect) is_boundary = TRUE;
++			if (cave_sector[y][x + 1] != sect) is_boundary = TRUE;
+
+ 			if (is_boundary)
+ 			{
+@@ -6040,12 +6046,9 @@ static void smooth_caverns(void)
+ 			for (x = 1; x < DUNGEON_WID - 1; x++)
+ 			{
+ 				int by = y / BLOCK_HGT;
+-				int bx = x / BLOCK_WID;
+-				if (by >= MAX_ROOMS_ROW) by = MAX_ROOMS_ROW - 1;
+-				if (bx >= MAX_ROOMS_COL) bx = MAX_ROOMS_COL - 1;
+
+ 				/* Only apply to SECTOR_CAVERN to maintain other sector structures */
+-				if (cave_sector[by][bx] != SECTOR_CAVERN) continue;
++				if (cave_sector[y][x] != SECTOR_CAVERN) continue;
+
+ 				/* Do not overwrite permanent features or stairs/doors */
+ 				if (cave_feat[y][x] >= FEAT_PERM_EXTRA || cave_feat[y][x] == FEAT_LESS || cave_feat[y][x] == FEAT_MORE || (cave_feat[y][x] >= FEAT_DOOR_HEAD && cave_feat[y][x] <= FEAT_DOOR_TAIL)) continue;
+@@ -6209,7 +6212,7 @@ static void cave_gen(void)
+ 	/* Initialize Elevation */
+ 	init_elevation();
+
+-	/* Initialize Sector Map to RUINS by default */
++	/* Initialize Sector Map to RUINS by default for all blocks */
+ 	for (y = 0; y < dun->row_rooms; y++)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x++)
+@@ -6331,7 +6334,7 @@ static void cave_gen(void)
+ 		if (room_build(y, x, 1))
+ 		{
+ 			/* If we successfully built a simple room in RUINS, try to cluster a neighborhood */
+-			if (cave_sector[y][x] == SECTOR_RUINS)
++			if (dun->sector_map[y][x] == SECTOR_RUINS)
+ 			{
+ 				int cy, cx;
+ 				for (cy = y - 1; cy <= y + 1; cy++)
+@@ -6376,17 +6379,26 @@ static void cave_gen(void)
+ 				else if (roll < 80) sect_type = SECTOR_PIT;        /* 5% Pit */
+ 				else sect_type = SECTOR_CAVERN;                    /* 20% Cavern */
+
+-			/* Assign to 2x2 block */
+-			cave_sector[y][x] = sect_type;
+-			if (y + 1 < dun->row_rooms) cave_sector[y+1][x] = sect_type;
+-			if (x + 1 < dun->col_rooms) cave_sector[y][x+1] = sect_type;
+-			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) cave_sector[y+1][x+1] = sect_type;
++			/* Assign to all blocks in the 2x2 sector group */
++			dun->sector_map[y][x] = sect_type;
++			if (y + 1 < dun->row_rooms) dun->sector_map[y+1][x] = sect_type;
++			if (x + 1 < dun->col_rooms) dun->sector_map[y][x+1] = sect_type;
++			if (y + 1 < dun->row_rooms && x + 1 < dun->col_rooms) dun->sector_map[y+1][x+1] = sect_type;
+ 		}
+ 	}
+
+ 	/* No "crowded" rooms yet */
+ 	dun->crowded = FALSE;
+
++	/* Sync Sector Map to Global Cave Sector Map (Tile-mapped) */
++	for (y = 0; y < DUNGEON_HGT; y++)
++	{
++		for (x = 0; x < DUNGEON_WID; x++)
++		{
++			cave_sector[y][x] = dun->sector_map[y / BLOCK_HGT][x / BLOCK_WID];
++		}
++	}
++
+
+ 	/* No rooms yet */
+ 	dun->cent_n = 0;
+@@ -6396,7 +6408,7 @@ static void cave_gen(void)
+ 	{
+ 		for (x = 0; x < dun->col_rooms; x += 2)
+ 		{
+-			int sect = cave_sector[y][x];
++			int sect = dun->sector_map[y][x];
+ 			if (sect == SECTOR_SHIFTING)
+ 				{
+ 					build_sector_shifting_maze(y, x);
+@@ -6643,17 +6655,6 @@ static void cave_gen(void)
+ 		int start_feat = (!p_ptr->depth) ? FEAT_MORE : FEAT_LESS;
+ 		if (!p_ptr->depth && p_ptr->inside_special == SPECIAL_WILD) start_feat = FEAT_SHAFT;
+
+-		for (sy = 0; sy < DUNGEON_HGT; sy++) {
+-			for (sx = 0; sx < DUNGEON_WID; sx++) {
+-				if (cave_feat[sy][sx] == start_feat) {
+-					set_generation_origin(sy, sx);
+-					found_start = TRUE;
+-					break;
+-				}
+-			}
+-			if (found_start) break;
+-		}
+-		if (!found_start) {
+ 			/* Fallback to center */
+ 			set_generation_origin(DUNGEON_HGT / 2, DUNGEON_WID / 2);
+ 		}
+@@ -6792,6 +6793,7 @@ static void cave_gen(void)
+ 	}
+
+ 	free(dun_body);
++	dun = NULL;
+ }
+
+
